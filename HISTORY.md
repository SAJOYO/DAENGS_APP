# HISTORY — 홈 화면 · 미니룸 작업 일지

이 문서는 이 저장소를 처음 보는 사람도 배경지식 없이 따라올 수 있게 쓴 작업 일지다.
2026-08-22 ~ 08-27, Claude Code 세션 기준. 지금 상태만 빠르게 보려면
[STATUS.md](STATUS.md) 를 보면 된다.

---

## 0. 이게 뭐 하는 저장소인가

댕스는 반려견 케어 플랫폼이다. AI 에이전트(훈련 상담·사진 분석)와 게임(미니룸·산책)이
같이 들어간다.

이 저장소는 그중 **안드로이드 앱의 홈 화면**, 특히 화면 한가운데를 차지하는
**미니룸**을 만든다. 방 안에 강아지가 있고, 소품을 놓고 옮기고, 문을 열면 산책을
나가는 그 부분이다.

**미니룸이란**: 위에서 비스듬히 내려다보는 방 한 칸. 게임에서 흔히 쓰는
아이소메트릭(등각 투영) 시점이다. 소품을 격자 칸에 놓고, 앞에 있는 물건이 뒤를
가리도록 순서를 맞춰 그린다.

병원·약국 찾기 지도는 다른 팀원이 맡는다. 여기서는 건드리지 않는다.

---

## 1. 왜 안드로이드 네이티브인가 (08-22)

유니티나 게임 엔진을 쓰지 않기로 했다. 미니룸은 화면 한 칸짜리 장식이고, 나머지
화면(챗봇·기록·커뮤니티)은 평범한 앱 UI다. 게임 엔진을 얹으면 앱이 무거워지고
평범한 화면을 만들기가 오히려 번거로워진다.

그래서 **Jetpack Compose 의 `Canvas` 에 직접 그리는** 방식으로 갔다. Compose 는
안드로이드의 최신 화면 만들기 도구이고, `Canvas` 는 그 안에서 원·사각형·이미지를
좌표로 직접 그리는 판이다.

## 2. 격자 엔진 — 좌표계부터 (08-22)

미니룸에서 제일 먼저 필요한 건 **격자 칸 ↔ 화면 좌표 변환**이다. "3번째 칸 2번째
줄"이 화면의 어디인지, 손가락이 짚은 화면 좌표가 몇 번 칸인지 서로 바꿀 수 있어야
소품을 놓을 수 있다.

이때 만든 규칙이 지금까지 남아 있다 — **`ArtBox`**. 아이템 하나마다

- **크기** (그림이 화면에서 차지하는 크기)
- **기준점** (그림의 어느 점이 바닥에 닿는가)
- **차지하는 칸** (몇 칸 × 몇 칸을 먹는가)

세 가지를 적어둔다. 이 셋만 있으면 배치·터치 판정·앞뒤 정렬·점유 계산이 전부
따라온다. **나중에 아트를 도형에서 PNG로 통째로 갈아엎을 때 이 계약 덕을 크게 봤다.**

### 기준점이 왜 따로 필요한가

물건이 공중에 떠 보이는 원인은 그림자가 아니라 기준점이었다. 바닥에 눕는 것(방석·
밥그릇)은 밑면 타원의 **중심**이 접지점이고, 서 있는 것(화분·강아지집)은 **맨 아랫점**이
접지점이다. 이 둘을 섞으면 어떤 건 뜨고 어떤 건 파묻힌다. 규칙을 나눠 적어둔 뒤로는
안 틀렸다.

## 3. 무료 에셋을 받았다가 되돌린 일 (08-22)

CC0(저작권 없는) 가구 킷을 받았다. 140종 × 4방향 = 561장. 각도가 우리 격자와 안 맞아서
방 전체 비율을 에셋에 맞춰 바꾸는 실험까지 했고, 실제로 상자 밑변과 바닥 격자선이
나란해졌다. 각도 문제는 풀린 것이다.

**그런데도 되돌렸다.** 문제가 각도가 아니라 내용물이었기 때문이다. 집 가구 킷이라
의자·책장은 있어도 **밥그릇·강아지집·장난감이 없었다.** 넣어보니 강아지 방이 아니라
인형의 집이 됐다.

> 배운 것: 에셋 팩은 "각도가 맞는가"보다 **"내용이 맞는가"를 먼저 봐야 한다.**
> 각도만 봤으면 재렌더에 시간을 더 쓰고도 각도 맞는 사람 가구를 얻었을 뿐이다.

이 과정에서 상식 하나도 바로잡았다. 아이소메트릭 자료에 자주 나오는 26.565°는
**화면상 모서리 각도이지 카메라 고도가 아니다.** 타일 가로세로 2:1 을 만들려면 30°다.
매번 손으로 재기 싫어서 **스프라이트 각도를 자동으로 재주는 도구**를 만들었다
(`tools/isoasset.py`).

## 4. 강아지를 격자에서 떼어내다 (08-23)

소품은 칸에 딱 맞게 놓이지만 **강아지는 칸 사이를 자유롭게 걸어야** 한다. 그래서
강아지만 격자에서 분리해 실수 좌표로 움직이게 했다.

여기서 규칙 하나를 새로 만들지 않은 게 좋았다. 강아지가 **가구는 못 지나가고
러그·방석 위로는 지나가는** 동작은, 배치할 때 쓰는 "칸 점유" 정보를 그대로 재사용해서
나온다. 아이템 하나를 "바닥에 깔리는 것"으로 바꾸면 배치와 통행이 같이 따라온다.

## 5. 실기기에서 결론이 뒤집힌 것들 (08-23)

에뮬레이터나 미리보기로는 안 보이던 게 실기기에서 드러났다.

- **프로시저럴 걷기가 반려됐다.** 강아지 아트는 AI 생성 예정인데 대기 동작과 걷기를
  따로 만들면 견종당 생성 횟수가 두 배가 된다. 그래서 정지 그림 한 장에 코드로 움직임을
  입혔다(통통 튀기 + 착지 시 눌리기). 실기기에서 "걷는 것처럼 안 보인다"가 나왔고,
  원인은 셋 다 코드였다 — 다리만 고정돼 제자리뜀으로 읽힘 / 여러 마리가 **같은 시계**를
  써서 숨쉬기가 완벽히 동기화(한 몸처럼 보임) / 너무 자주 돌아다님
- **네발 걸음은 옆에서 봐야 읽힌다.** 정면은 보이는 다리가 둘뿐이라 아무리 잘
  움직여도 두 발 걷기가 되어 "사람 같다"는 반응이 나왔다 → **쉴 땐 정면, 걸을 땐
  옆모습**으로 나눴다
- **교과서 값을 그대로 쓰면 안 됐다.** 귀여움은 비율에서 나온다는 이론(Kindchenschema)
  대로 눈을 머리 높이의 1/4 로 잡았더니, 130px 화면에서 두 번 "부담스럽다"가 나와
  20%까지 줄였다. 이론값은 큰 화면 기준이고 우리는 손톱만 하게 그린다

### 제일 오래 걸린 버그

**"그림은 맞는데 터치만 안 되는"** 증상이었다. 편집 모드에서 가구가 눈에는 제대로
보이는데 눌러도 안 잡혔다. 그리는 쪽은 매번 새로 평가돼 정상 반영되고, 터치 쪽만 첫
화면을 그릴 때의 옛 값에 묶여 있었기 때문이다. 화면상으로는 절반이 동작하는 것처럼
보여서 원인 방향을 잘못 잡았다.

> 배운 것: **같은 상태를 두 경로가 다르게 읽고 있는지부터 본다.**

## 6. 화풍을 세 번 갈아엎다 (08-23 ~ 08-24)

강아지를 어떤 그림체로 그릴지 정하려고 세 판본을 만들었다.

| | 화풍 | 결과 |
|---|---|---|
| V1 | 파스텔 벡터 도형 | 반려 |
| V2 | 도트(픽셀)를 격자에 직접 찍기 | 반려 |
| V3 | 잉크 만화 (굵은 윤곽선) | 반려 |

세 번 다 반려됐다. 그 과정에서 나온 것들:

- **"알파카" 사건.** 다리가 떠 보이길래 몸통을 키웠더니 목이 길어져 알파카가 됐다.
  맞는 방향은 정반대 — 몸통을 줄이고 전체를 낮추는 것이었다
- **걸을 때 대머리가 됐다.** 귀가 뒤로 날리는 느낌을 주려고 귀를 머리보다 **먼저**
  그렸더니 머리가 귀를 덮어버렸다. 같은 실수를 V1 에서 이미 하고 주석까지 남겼는데
  V2 에서 또 했다
- **축소 ≠ 도트.** 벡터 그림을 그냥 작게 줄여놓고 도트라고 했더니 "화질만 나빠진
  느낌"이라는 지적을 받았다. 진짜 도트는 정수 격자에 직접 찍어야 한다

> 세 번 만들어 보고 나서야 **"우리가 직접 그려서 될 게 아니다"** 가 확실해졌다.
> 낭비처럼 보이지만, 이 확신이 있어서 다음 결정이 빨랐다.

## 7. 남의 시스템인 줄 알았는데 우리 것이었다 (08-24)

네 번째로 손댄 게 팀 다른 저장소(`frankie516c/dog-training-rag`)에 있던 **픽셀 아트
PNG 와 그걸 돌리는 브라우저 목업**이었다.

그 목업의 물리 코드를 열어보니 주석에 **우리 저장소 주소가 적혀 있었다.**

```js
// Browser port of the grid-footprint and axis-slide approach from
// https://github.com/gayeoniee/isometric_test
```

저쪽이 우리 좌표 로직을 웹으로 가져간 것이었다. 즉 이 작업은 **새 시스템 이식이 아니라
우리 로직에 저쪽이 입힌 아트를 되가져오는 것**이었다. 뼈대가 이미 같으니 범위가 확
줄었다.

바꾼 건 **바닥 기하 하나**다. 우리 좌표계는 화면 중앙에 놓인 순수 마름모였는데, 저쪽은
**방 그림에 픽셀 단위로 자를 대서 맞춘 사각형**이었다. 방이 그림 한 장이 된 이상 격자가
그림에 맞아야 하므로 이쪽으로 갈아탔다. 격자 수는 저쪽 16, 우리 12라 칸 단위 값(속도·
충돌 반경)만 12/16 배로 환산했다.

**이 판본이 지금의 V3 이고, 확정본이다.**

## 8. 색으로는 자를 수 없는 것 (08-24)

방 PNG 는 배경이 불투명한 황토색이라 그대로 얹으면 앱 테마색을 가린다. 바깥을 투명하게
뚫어야 했다.

색만 보고 판정했더니 **그늘진 왼쪽 벽이 통째로 뜯겼다.** 벽 그늘과 배경의 밝기가
겹쳐서다. "픽셀이 기준색의 배수인가"(밝기만 다르고 색조가 같은가)를 보는 조건을 넣어
갈랐다.

진짜 벽은 테마를 6종으로 늘릴 때 나왔다. 파스텔 테마는 벽 색이 배경색과 거의 같아서
같은 방법을 쓰면 벽에 큰 구멍이 뚫린다. 색을 넓게 잡아 봤더니 이번엔 오른쪽 벽이
먹혔다.

> **색으로 가를 수 없는 자리라고 결론 내고 방법을 바꿨다.** 실루엣을 테마마다 구하지
> 않고, 배경과 벽이 뚜렷이 다른 원본 한 장에서만 구해 전부에 씌웠다. 테마는 같은
> 그림을 리컬러한 것이라 실루엣이 전부 같다는 성질을 쓴 것이다.

불투명 비율이 테마별 63~68% 에서 전부 79.2% 로 맞았다.

## 9. 문 하나에 세 번 틀리다 (08-25)

문은 눌러서 여는 물건이라 그림에 구워둘 수 없다. 그렇다고 문만 벡터로 새로 그리면
픽셀 아트 방에 매끈한 도형이 얹혀 화풍이 깨진다. 그래서 **같은 PNG 에서 문짝만 오려
경첩 쪽으로 눌러 그리기로** 했는데, 세 번 틀렸다.

1. **네모로 다뤘다.** 문이 아치라 네모의 남는 모서리는 전부 벽인데, 그 자리를 어둡게
   칠하니 검은 딱지가 붙은 꼴이 됐다
2. **타원으로 근사했다.** 반원 아치로 그렸더니 실제 문보다 볼록해서 양옆이 문 위까지
   열렸다
3. **어둠을 문간으로 썼다.** 모양을 맞춰도 "나가는 느낌이 안 난다"가 나왔다

> **어둠은 막힌 곳이지 나갈 곳이 아니다.** 같은 그림의 창유리를 오려 늘려 하늘·먼 산·
> 나무가 보이게 하고, 아래쪽은 잔디와 볕 든 흙길로 덮었다. 나갈 땅이 있어야 나가는
> 문이 된다. 바닥에도 볕이 번지게 했다.

결국 **그림에서 문 윤곽을 그대로 떠서** 쓴다. 가로로 21등분해 위·아래 가장자리를 재고
그 값을 이어 그린다. 여기서 함정이 하나 있었다 — 문 색을 밝은 올리브로만 잡으면
그늘진 아치 꼭대기를 놓쳐 윤곽이 문 안쪽으로 파인다. 문의 표식은 밝기가 아니라
**빨강과 초록이 비슷하고 파랑이 빠졌다는 것**이었다(벽과 문틀은 붉은기가 돈다).

다시 뜰 일이 있을 테니 도구로 만들어 뒀다 (`tools/trace_door.py`). 뽑은 윤곽을 그림
위에 분홍 선으로 얹어 내보내니 눈으로 확인하고 넣으면 된다.

## 10. 규격이 같다고 덩치가 같은 건 아니다 (08-25)

견종 시트는 전부 2328×568 짜리 4프레임이라 규격이 같다. 그걸 근거로 화면 폭까지 하나로
통일했는데, 그러면 **치와와와 시베리안 허스키가 같은 크기로 선다.**

저쪽 브랜치를 뒤져 보니 견종마다 **세 값을 따로** 잡아두고 있었다 — 보이는 폭, 바닥에서
차지하는 반경, 걷는 속도. 그대로 가져왔다. 폭은 9.5% ~ 16.5% 로 1.74배 차이가 난다.

**반경이 폭과 따로 있는 게 핵심이다.** 비숑처럼 털이 부푼 견종은 실루엣이 커도 실제로
부딪히는 몸은 그만큼 크지 않다. 폭에서 반경을 계산해내면 이 구분이 사라진다. 폭이 같은
포메라니안과 퍼그의 반경이 다른지 보는 테스트를 넣어, 나중에 누가 한쪽에서 다른 쪽을
계산하려 들면 걸리게 했다.

배선에서 두 군데 걸렸다.

- 강아지 객체가 속도·반경을 **만들 때 복사해 두면** 개발자 도구로 견종을 바꿔도 이전
  견종 속도로 걷는다 → 견종에서 바로 읽게 했다
- 막힘 판정도 마리마다 반경을 받아야 한다 → 래브라도가 지나갈 통로를 치와와 반경으로
  재면 큰 개가 가구를 파고든 채 멈춘다

## 11. 눈이 3픽셀이면 어떻게 되는가 (08-25)

저쪽에서 그림을 갱신했다(시바 색, 비숑 털결, 보더콜리 눈). 반입하고 나서
**"보더콜리 눈이 무섭다"** 는 지적이 나왔다.

재보니 이랬다.

| | 밝기(0~255) |
|---|---|
| 눈 둘레 털 | 40~58 |
| **눈 속** | **0~16** |
| 흰 반짝임 | 240 |

**눈이 얼굴보다 어두우니 파인 구멍으로 읽히고**, 그 안의 흰 반짝임만 떠서 그게
눈알처럼 보인 것이다. 다른 15종은 얼굴이 밝아 검은 눈이 또렷하고, 허스키는 파란 눈이라
검은 얼굴에서도 읽힌다.

근본 원인은 크기다. **원본 프레임 582px 가 화면에서 126px 로 그려진다(4.6배 축소).**
그러면 눈이 3~5px 이고, **색은 사라지고 명도만 남는다.** 저쪽 목업은 방 크기가
`width: 80vh` 라 큰 모니터에서는 훨씬 크게 나와서, 같은 그림이 저쪽에서는 멀쩡해 보인다.

후처리로 고치려고 시도한 것과 결과:

| 시도 | 결과 |
|---|---|
| 흰 반짝임 깎기 | ✗ 축소하면 깎은 게 사라진다 |
| 축소 필터 바꾸기(면적평균·LANCZOS) | ✗ 눈은 거의 그대로 |
| 홍채 어둡게 | ✗ 구멍만 더 깊어진다 |
| 눈 속을 **밝게** | ○ 안구로 읽힌다 |

마지막 것이 통했지만, **원본을 게임 크기에 맞춰 다시 그리는 게 맞다**고 판단해 저쪽에
요청하고 실험은 전부 되돌렸다.

> 배운 것: 새 그림이 오면 **화면 크기로 줄여서** 판단한다. 원본 크기로만 보면 문제가
> 안 보인다.

## 12. 도감을 네이티브로 (08-26 ~ 08-27)

팀 저장소 `SAJOYO/DAENGS_dev` 에 **홀로그램 트레이딩 카드 도감**이 만들어져 있었다.
강아지 네오가 채소가 된 카드 12장을, 포켓몬 카드처럼 기울이면 반짝이게 만든 것이다.

먼저 **그 웹 데모를 통째로 앱에 싣고 WebView 로 띄웠다.** 포일 12종이 CSS 블렌드
모드(`color-dodge` · `hard-light`)와 다중 그라디언트를 포인터 위치로 굴리는 것이라,
네이티브로 옮기면 *옮기는* 게 아니라 *다시 유도하는* 일이 되기 때문이다.

그러다 **앱으로 통일하기로 하고 Compose 로 다시 만들었다.** 해보니 생각보다 할 만
했는데, 이유가 있었다 — **카드 그림에 프레임·제목·수치가 전부 구워져 있어서** 다시
만들 게 카드 내용이 아니라 효과뿐이었다.

CSS 를 이렇게 옮겼다.

| CSS | Compose |
|---|---|
| `background-image` | `Brush` (linear · radial · sweep) |
| `background-blend-mode` | `saveLayer` 안에서 두 번째 brush 를 `blendMode` 로 |
| `filter` | `ColorFilter.colorMatrix` (밝기·대비·채도를 행렬 하나로) |
| `mix-blend-mode` | `saveLayer` 의 `Paint` 에 `blendMode` |

없는 건 `repeating-conic-gradient` 하나뿐이었다. `sweepGradient` 는 한 바퀴만 돌아서,
60도짜리 무늬면 색을 여섯 번 이어 붙여 360도를 채웠다.

### 세기를 두 번 잡았다

처음 얹었더니 **얼굴이 통째로 날아갔다.** 저쪽 README 경고 그대로다 — `color-dodge`
는 밝은 원화에서 흰색으로 클리핑되고, 제일 먼저 사라지는 게 흰 강아지 얼굴이다.

### 옮기면서 배운 것

- **카드 높이를 맞추고 폭만 비율대로.** 그림 비율이 두 종류(0.72 · 0.80)라 폭을
  맞추면 줄이 어긋난다. 저쪽이 "실물 카드 바인더와 같은 정렬" 이라 부르는 배치다
- **문지르기가 스크롤을 먹으면 안 된다.** 카드를 짚고 쓸어내릴 때 목록이 안 움직였다
- **배경은 cover 여야 한다.** 원화가 가로로 긴데 가로만 맞췄더니 밭이 화면의 31% 짜리
  띠로 남았다
- **게이지는 카드 모서리를 따라야 한다.** `drawArc` 는 사각형에 내접한 타원을 그린다 —
  카드와 상관없는 선이라 "카드가 반응한다"로 안 읽힌다

### 그러다 설명 시트를 빠뜨렸다 (09-01 에 되살림)

"다시 만들 게 카드 내용이 아니라 효과뿐" 이라고 본 것이 반만 맞았다. **그림에 안
구워진 것이 하나 있었다 — 글이다.** 한글 이름·부제·코드·플레이버·에디션은 카드
어디에도 인쇄돼 있지 않아서, 웹판은 카드를 두 번 누르면 뜨는 시트로 보여 주고
있었다. 그 시트가 통째로 빠진 채 넉 달을 갔고, 사용자가 "원래 있었는데" 라고
말해서야 알았다.

**그림에 있는 것만 보고 "옮길 게 없다" 고 판단하면 이렇게 된다.** 다음에 저쪽에서
무엇을 옮길 때는 화면에 뜨는 것과 데이터에 있는 것을 따로 세는 편이 낫다.

되살리면서 하나 더 나왔다 — **탭 제스처가 이벤트를 안 먹고 있었다.** 문지르기만
먹어서, 카드를 누르면 뒤에 깔린 "밖을 눌러 닫기" 가 같은 탭으로 같이 발동했다.
설명을 열자마자 뷰어가 닫혔다.

## 13. 팀 저장소로 (08-25)

지금까지는 개인 저장소에서 혼자 만들었다. 팀 org 에 `SAJOYO/DAENGS_APP` 이 생기면서
현재 상태를 **기준**으로 올렸다. 커밋 히스토리는 전부 그대로 옮겼다 — 왜 이렇게
만들었는지가 메시지에 남아 있어서다.

앞으로는 **실험은 개인 저장소에서, 정리되면 팀으로** 올린다.

---

## 14. 얼굴 그림 (08-27)

저쪽이 견종마다 얼굴 그림을 따로 그려 두었는데, 우리는 걷기 시트만 가져오고 이건
안 쓰고 있었다. 견종을 고르는 화면이 생기면 그때 쓰자고 미뤄둔 것이었다.

그 사이 프로필 아바타는 Canvas 로 원과 타원을 얹어 그린 갈색 강아지 한 마리였다.
견종이 25종인데 아바타는 하나였으니, 25장을 다 들여와 아바타를 그 그림으로 바꿨다.

**화풍이 다르다.** 방 안 강아지는 도트, 얼굴은 사실풍이다. 처음엔 섞이는 게 걸렸는데
아바타가 서는 자리가 32dp 라 도트로는 눈코가 먼저 뭉개진다. 저쪽이 나눠 그린 이유가
그거였을 것이다.

원본이 크림색 불투명 정사각형이라 동그랗게 자르고 테두리를 한 겹 둘렀다. 안 두르면
밝은 카드 위에서 배경이 경계 없이 번진다.

대표 견종은 `HomeDemoData.DOG_BREED` 한 곳에 박아뒀다. 사용자가 고르는 화면이
생기면 이 상수만 걷어내면 된다.

같은 회차에 시바 세 색이 서로 다른 그림이던 걸 저쪽이 다시 그려 보냈다 (베이지
원본에서 색만 돌린 것). 걷기 시트는 알파가 프레임 4장 모두 완전히 일치하는 것으로,
얼굴 그림은 배경에서 떼어낸 실루엣이 2.4% 안에서 겹치는 것으로 확인했다 — 다른
견종끼리는 48% 어긋난다.

---

## 15. 창을 통과해 들어가기 (08-27)

저쪽(`SAJOYO/DAENGS_dev` PR #21)이 도감 진입 연출을 바꿔 왔다. 원래는 카드가 커지면서
통째로 녹았는데, 이제 **카드 그림만 녹아 틀이 드러나고 그 틀의 창이 벌어지면서 화면을
삼킨다.** 창을 통과해 안으로 들어가는 것으로 읽힌다.

그림이 두 장 필요했다. 하나는 카드에서 **그림 영역만 투명하게 지운 틀**이고, 하나는
넓어진 배경이다. 틀은 카드 그림 **아래에** 같은 자리로 깔려 있다가, 위의 그림이 녹으면
드러나서 창틀이 된다.

### 창은 그림창보다 커야 한다

저쪽이 겪은 것을 그대로 물려받았다. 틀에서 뚫린 데는 그림창 하나가 아니다. 원래 카드
에서 **그림 위에 얹혀 있던 반투명 판**들이 있어서, 그림을 지울 때 뒤가 같이 비었다.
그림창만 재서 넣으면 그 판들이 있던 자리가 검게 남는다.

그래서 창을 "뒤가 비치는 자리 **전부**"의 경계상자로 잡는다. 창이 뚫린 데보다 커도
상관없다 — 넘치는 만큼은 틀의 불투명한 부분이 가린다. 모양은 틀의 알파가 잡고, 이
상자는 받쳐 줄 범위만 정한다. 다만 카드 바깥 둥근 모서리까지 삼키면 안 된다. 받쳐
주면 둥근 귀퉁이로 텃밭이 새어 나와 카드가 직사각형으로 보인다.

### 얼마나 벌려야 하는지는 화면마다 다르다

창이 화면을 다 덮는 배율을 **고정값으로 박으면 안 된다.** 창은 카드 그림 영역이라
거의 정사각인데 화면은 세로로 길다. 같은 배율로 벌리면 한쪽이 먼저 끝나고 다른 쪽이
한참 남아서, 연출이 끝날 때까지 옆에 검은 띠가 남는다.

창은 자기 중심에서 벌어지므로, **중심에서 먼 쪽 변까지의 거리를 반너비로 나눈 값**이
곧 필요한 배율이다. 화면 네 변에 대해 구해서 가장 큰 것을 쓴다. 거기서 1.22배 더
벌린다 — 딱 덮는 데서 멈추면 "가려졌다"이지 "통과했다"가 아니다.

### 카드가 다 서고 나서 열어야 한다

창 자리는 카드 자리에서 나온다. 카드가 아직 움직이는 중에 창을 열면 **창 자리가 매
프레임 달라져서** 틀과 창이 어긋난다. 그래서 구간을 나눴다 — 44%까지 카드가 서고,
30~62%에 그림이 녹고, 62%부터 창이 열린다. 저쪽이 판을 44%에 세워 두는 이유가 이것이다.

### `touch.css` 는 안 가져왔다 — 재보니 해당이 없다

같은 PR 에 터치 기기용 효과 예산(`touch.css`, 323줄)이 들어 있었다. 안드로이드 크롬
에서 도감을 굴리면 카드 자리가 잠깐 비거나 회색으로 뜨는 문제를 잡은 것이다. 원인은
카드마다 `mix-blend-mode` + `filter` 를 가진 레이어가 6~7장 쌓여 있어서, 그 조합이 GPU
합성 경로를 못 타고 레이어마다 독립 render surface 로 따로 구워지는 데 있었다.

**우리는 그 구조가 아니다.** Compose 로 옮기면서 포일 하나를 `foilLayer` 2~3개로 접었고,
카드 한 장이 `CompositingStrategy.Offscreen` 한 겹 안에서 끝난다. 크롬의 타일 폐기라는
현상 자체도 없다.

그래서 옮기기 전에 재봤다.

| | 웹 (저쪽이 잰 값) | 우리 Compose |
| --- | --- | --- |
| 그리드 스크롤 중앙값 | 12ms | **6ms** |
| 90분위 | 19ms | **9ms** |
| 입력 지연 | 1453 | **373** |
| 확대 뷰 문지르기 90분위 | — | 13ms (놓친 vsync 0) |

고칠 것이 없다. 안 가져오는 편이 맞다 — 가져오면 이유 없이 효과만 얇아진다.

배경 그림(`cabbage-back.webp`)이 넓어진 것은 같이 받았다. 1339x670 에서 1376x768 이
되어 세로가 늘었다. 예전에 밭이 띠처럼 잘리던 것을 덮기 배율로 막아 뒀는데, 원화가
넓어지면 덜 잘라내도 된다.

### 들어가 보니 밭이 안 보였다

연출을 다 붙이고 실기기에서 보니 두 가지가 걸렸다.

**배추가 화면에 꽉 꼈다.** 주인공 폭이 화면의 105%라 양옆으로 넘쳤다. 그러면 밭에 서
있는 게 아니라 배추 사진 위에 앉은 것처럼 보인다. 70%로 줄이니 사방에 밭이 남아서
그제야 "안에 들어와 있다"가 됐다. 무대는 배경을 보여 주려고 만든 것인데 주인공이
그 배경을 다 가리고 있었다.

**앞잎사귀 층을 뺐다.** 저쪽 웹판에는 앞에 크게 흐리게 지나가는 잎이 있고, 초점이
안쪽에 맞은 것처럼 보이게 하는 층이다. 우리는 그걸 **큰 타원**으로 흉내 냈는데,
잎으로 안 읽히고 화면 아래쪽에 어두운 덩어리로 남아 흙을 뭉갰다. 원화 없이 도형으로
대신할 수 있는 층이 아니었다. 그림으로 그린 잎이 오면 그때 다시 본다.

둘 다 배경을 가리는 문제였다. 배경을 넓은 원화로 바꾼 회차에 배경이 제일 안 보였다.

### 겉잎을 겹으로 세워 두께 주기

저쪽이 "입체감을 주려고 사진을 세 장 겹쳤다"고 해서 확인해 보니, **서로 다른 사진
세 장이 아니었다.** 같은 누끼를 동심원 고리로 오려 앞으로 띄운 것이다. 속 한 장에
중간 잎(z 34) · 바깥 잎(z 70) 두 겹을 얹는다. 기울이면 각 겹이 속과 다른 속도로
어긋나고, 그 어긋남이 배추의 두께가 된다.

우리는 누끼 한 장을 평평하게 그리고 있어서 이 구조가 통째로 빠져 있었다. 옮겼다.

CSS 를 Compose 로 바꾼 대응은 셋이다.

| 저쪽 | 우리 |
| --- | --- |
| `mask: radial-gradient(circle at 50% 46%, ...)` | `Brush.radialGradient` 를 `BlendMode.DstIn` 으로 |
| `translateZ(z) scale(k)` | 시차만 더 준다 (아래) |
| `drop-shadow(...)` | 누끼를 검게 칠해 같은 고리로 오려 오른쪽 아래로 |

**`translateZ` 는 크기를 안 바꾼다.** 저쪽은 원근(1500px)이 키우는 만큼을
`scale((1500-z)/1500)` 으로 정확히 되돌린다. 남는 것은 회전할 때의 어긋남뿐이다.
그래서 우리는 크기를 건드리지 않고 시차만 더 줬다 —
`Par.SUBJECT + z × sin(9°) × (우리 누끼 높이 / 560)`. 560 은 저쪽 `--hh` 상한이라
z 값이 그 높이를 기준으로 잡혀 있다.

세로는 운이 좋았다. 저쪽이 가로 9도 · 세로 6도로 기울이는데 그 비가 0.67 이고,
우리 `parallax` 의 세로 계수가 이미 0.68 이라 따로 나눌 게 없었다.

블러는 안 걸었다. Compose 의 흐리기는 API 31 부터인데 우리 minSdk 는 26 이다.
대신 고리 마스크가 이미 넓게 페이드돼 있어서 그림자 경계가 충분히 부드럽다.
마스크를 딱 자르면 안 되는 이유도 같다 — 어긋나는 순간 "두께"가 아니라 "인쇄가
밀린 두 장"으로 보인다.

실기기에서 좌우로 기울여 놓고 **각 겹이 몇 픽셀 움직였는지 쟀다.**

| | 예상 비율 | 잰 비율 |
| --- | --- | --- |
| 속 : 중간 잎 : 바깥 잎 | 1 : 1.14 : 1.28 | **1 : 1.13 : 1.28** |

배경이 16px, 속이 76px, 바깥 잎이 97px 움직였다. 층이 의도대로 갈린다.

**아직 진짜 레이어는 아니다.** 겹이 속과 같은 픽셀이라 비켜도 새로 드러나는 게 없다.
저쪽도 같은 상태이고 README 에 그렇게 적어 뒀다. 겹마다 그린 원화가 오면 고리
마스크만 빼고 그림을 갈아 끼우면 되고, 나머지 구조는 그대로 쓴다.

### 어지러웠던 진짜 이유는 겹이 아니었다

겹을 넣고 나서 "좀 어지럽다"는 말을 들었다. 겹을 약하게 줄이려다 먼저 재봤는데,
원인이 딴 데 있었다.

**폰을 책상에 놓아둔 채로 1초 사이에 주인공이 46px 밀렸다.** 아무도 안 건드렸는데
화면이 저 혼자 떠다니고 있었다. 겹이 문제가 아니라 기울기 입력이 문제였다.

두 가지가 겹쳐 있었다.

- **거르는 게 하나도 없었다.** 회전벡터 센서는 가만히 있어도 조금씩 흔들리는데,
  그 눈금을 그대로 화면 좌표로 썼다. 손떨림이 곧바로 픽셀이 됐다.
- **`RANGE` 가 20도였다.** 저쪽 카드 값을 그대로 가져온 것인데, 카드는 손바닥만 한
  것이 번쩍일 뿐이라 20도가 알맞다. **이머시브는 화면 전체가 움직인다.** 같은 각도에
  같은 비율로 움직이면 멀미가 난다.

그래서 지수 이동평균(시정수 약 130ms)으로 눌러 주고, 이머시브만 범위를 40도로 벌렸다.
일부러 기울이는 동작은 시정수보다 훨씬 느려서 그대로 따라가고 떨림만 깎인다.

| | 이전 | 지금 |
| --- | --- | --- |
| 폰을 안 건드렸을 때 1초 동안 밀린 양 | **46px** | **0px** (3초 동안) |
| 손가락으로 문질렀을 때 겹의 비율 | 1 : 1.13 : 1.28 | **그대로** |

효과는 하나도 안 잃고 어지러움만 걷혔다. 확대 뷰의 카드도 같은 다듬기를 받는다
(범위는 20도 그대로 — 거기는 카드 한 장이라 저쪽 값이 맞다).

**"효과가 과하다"는 말이 나오면 효과부터 줄이지 말 것.** 이번엔 효과가 아니라
입력이 떨고 있었다.

### 남은 건 속도가 아니라 잔상이었다

떨림을 잡고 나니 "속도는 좋은데 **잔상** 같은 게 더 크다"는 말이 돌아왔다. 기울인
화면을 크게 놓고 보니 그대로였다 — 잎 가장자리 옆에 어두운 띠가 있고, 그 너머로
**같은 잎이 한 번 더** 나온다. 겹이 속과 같은 픽셀이라 밀리면 그림이 두 번 그려진다.
저쪽 README 가 "인쇄가 밀린 두 장으로 보인다"고 경고한 바로 그 자리다.

세 가지를 차례로 해 봤다.

**1. 그림자에 흐리기.** 우리는 블러가 API 31 부터라 안 걸고 있었는데, 딱 떨어지는
검은 복사본을 몇 px 밀어 놓은 게 어두운 띠의 정체였다. `RenderEffect` 대신 **다섯
장을 조금씩 어긋나게 겹쳐** 흉내 냈다. 버전을 안 타서 좋다. 띠는 옅어졌지만 겹쳐
보이는 것 자체는 남았다.

**2. 겹을 반투명하게.** 밀린 복사본이 속에 녹아들 거라고 봤는데 **더 나빠졌다.**
녹는 게 아니라 이중노출처럼 번져서, 두께가 아니라 초점이 나간 것으로 보였다. 되돌렸다.

**3. 어긋나는 거리를 줄이기.** 이게 답이었다. 저쪽 `z` 값(34 · 70)을 절반(16 · 32)으로
줄이니 두 겹으로 보이던 윤곽이 사라졌다. 그러면서도 겹은 여전히 속보다 5px · 10px
더 움직인다.

| | 겹이 속보다 더 움직인 거리 | 잔상 |
| --- | --- | --- |
| 저쪽 값 그대로 (z 34 · 70) | 10px · 21px | 잎이 두 번 보인다 |
| **절반 (z 16 · 32)** | **5px · 10px** | **안 보인다** |

비율은 잰 값이 설계값과 정확히 맞는다 (1 : 1.066 : 1.132 대 1 : 1.065 : 1.13).

**저쪽 값을 그대로 쓸 수 없는 경우였다.** 저쪽은 카드 한 장 안에서 도는 연출이고
우리는 화면 전체다. 같은 비율이어도 절대 픽셀이 커지면 같은 결함이 더 크게 보인다.
`RANGE` 20도를 그대로 가져와서 어지러웠던 것과 같은 종류의 실수다.

---

## 16. 방이 상자 밖으로 잘리던 것 (08-27)

방 그림이 **상자보다 크게 그려진 뒤 넘치는 만큼 잘려** 나가고 있었다. 일부러 그렇게
해둔 것이다 — `OVERSCAN` 1.10 으로 키우고 `BOTTOM_BLEED` 0.07 로 아래를 밀어냈다.
주석에도 이유가 적혀 있었다. 방 그림이 세로로 길어서(1122x1402) 가로로 넓은 상자에
통째로 넣으면 좌우에 큰 여백이 남으니, 키워서라도 폭을 쓰겠다는 것이다.

문제는 **잘려 나가는 쪽이 하필 바닥 앞모서리 · 흰 굽도리 · 울타리 밑동**이라는 데
있었다. 벽 위 몰딩이 잘리는 건 티가 안 나지만 앞쪽은 방이 잘린 티가 확 난다.
실기기 스크린샷에서 재보니 390x844 기준으로 위 8.4px · 아래 28px 이 사라지고 있었다.
바닥 앞 꼭짓점이 상자 밑변보다 6px 아래였다.

`INSET`(0.94)과 `V_BIAS` 로 바꿨다. 짧은 축에 맞춘 뒤 물러나는 방식이라 **잘림이 없는
게 값을 잘 골라서가 아니라 식의 성질이다** — `s` 가 `contain` 을 넘을 수 없다.

### 위가 넓어 보이던 것

`V_BIAS` 를 0.45 로 두고 "가운데보다 살짝 위"를 의도했는데 화면에서는 반대였다.
**방 PNG 위쪽 39px 이 통째로 투명이다.** 공칭 사각형을 기준으로 놓으니 그 39px 이
위 여백에 얹혔다.

| | 위 | 아래 |
| --- | --- | --- |
| 계산값 | 8.2 | 10 |
| **실제로 보이는 것** | **16.1** | **10** |

정렬 기준을 공칭 사각형에서 **칠해진 영역**으로 옮기고 `V_BIAS` 를 0.40 으로 다시
잡았다. 이러면 이 값이 곧 눈에 보이는 위:아래 비가 된다. 에뮬레이터 실측 12.6 / 18.7.

투명 여백은 여섯 테마가 모두 같다 — 한 원본을 `build_room_theme_assets.ps1` 이 색만
바꿔 찍어내기 때문이다. 그래서 `ART_PAD_*` 상수로 박았다. **새 방 아트를 넣으면 반드시
다시 재야 한다.** 안 재면 방이 몇 px 밀릴 뿐 터지지 않아서 알아채기 어렵다.

### 이름표가 방 밖에 떠 있던 것

방이 상자보다 작아지자 `BottomEnd` 정렬이 성립하지 않았다. 이름표 폭 97.9dp 중
**62.5dp(64%)가 방 밖 배경 위**에 있었다. 방 크기는 화면·카드 높이에 따라 계속 변하니
상자 기준으로는 맞출 수가 없다. 방 그림 기준 백분율(`NamePlateSpec`)로 옮겼다.

### 카드를 줄여 방을 키우는 안을 셋 만들어 보고 접었다

방은 세로에 걸려서 크기가 정해지므로, 키우려면 카드를 줄여 상자에 세로를 주는 수밖에
없다. 셋을 실제로 빌드해 에뮬레이터(Pixel 7, 411x914dp)에서 재봤다.

| | 카드 합계 | 방 상자 | 방 그림 | 좌우 여백 |
| --- | --- | --- | --- | --- |
| **A (현행)** | 332.1 | 360.4 | 270.1 x 329.1 | **70.9** |
| B (챗봇 120) | 306.1 | 386.3 | 289.5 x 352.8 | 61.0 |
| C (B + 산책 -12) | 294.1 | 398.9 | 299.0 x 364.6 | 56.4 |

**챗봇 카드는 여유가 0 이다.** 146dp 안에서 콘텐츠가 123.4dp 를 쓰고 남은 22dp 는
정확히 `padding(vertical = 11.dp)` 위아래다. 그래서 120 은 설정값 변경이 아니라
입력창(44->38) · 칩 패딩(8->4) · 스페이서를 잘라내는 작업이다. 터치 영역이 칩 36.2 ->
28.2dp 로 줄어든다.

**그런데 셋 다 시안 프레이밍에 못 간다.** 방 보이는 폭은 상자 높이에 정비례한다.

```
방 보이는 폭 = 0.7496 x 상자 높이      (실측 360.4 x 0.7496 = 270.2, 잰 값 270.1)
```

시안처럼 좌우 20dp 만 남기려면 상자가 **495dp** 여야 한다. 지금 360dp 이고, 카드 두 장
합계가 332dp 다. **카드를 다 없애도 모자란다.**

그래서 **A 를 유지한다.** B 는 전체 이득의 3분의 2 를 가져가지만 그 이득이 방 폭 7%
증가이고, 대가는 터치 영역이다. 목표에 어차피 못 갈 거라면 카드를 훼손하면서 중간에
설 이유가 약하다. 목표 프레이밍은 **가로형 방 에셋을 새로 그리거나 화면 구조를 바꾸는
별도 과제**로 분리했다 (STATUS.md "다음 결정").

### 재는 방법

Layout Inspector 는 GUI 전용이라 CLI 에서 못 돌린다. 값마다 다르게 쟀다.

| 값 | 방법 |
| --- | --- |
| 상태바 51.8dp · 내비바 24.1dp | `adb shell dumpsys window displays` 의 `overrideNonDecorInsets` |
| 탭 라벨 22.1dp | `adb shell uiautomator dump` 의 텍스트 노드 경계 |
| 카드 높이 · 방 여백 | 스크린샷 픽셀 측정 (배경과 카드·방의 색 경계) |

방 상자 윗변은 세 방법이 서로 맞는지 봤다 — TODAY 카드 최상단에서 10dp 빼기,
상태바에 탭 라벨 실측을 더해 조립하기, fit 식에서 역산하기. 앞의 둘이 293.8px 로
정확히 같았다.

**하단 크롬에서 `FabLift` 22dp 를 빠뜨리고 있었다.** `DaengsBottomBar` 의 Surface 가
`padding(top = FabLift)` 를 쓰므로 바깥 Box 높이에 그대로 더해진다. 실측 110.1dp =
64 + 22 + 24.1 로 맞는다. 계산으로만 했으면 계속 22dp 를 틀리게 잡고 있었을 것이다.

## 17. 창밖이 하루 종일 정오이던 것 (08-28 ~ 08-29)

방 그림에 낮·맑음 풍경이 **구워져** 있었다. 밤에 앱을 켜도 창밖은 정오였다.
시간(낮·밤) x 날씨(해·비·눈) 여섯 벌로 늘리고, 실제 시각·날씨를 따라 고르게 했다.

### 서른여섯 장이 아니라 여섯 장이다

방 그림은 테마 6종이다. 그대로 곱하면 36장인데, 테마 여섯을 겹쳐 재 보니
**유리 안 풍경은 6종이 픽셀 단위로 똑같았다.** 테마마다 다른 것은 창틀·창살(테마색
나무)뿐이다. 풍경 한 벌이 테마 전부를 덮는다.

이건 그리는 양만 줄인 게 아니라 **유리 마스크를 공짜로 줬다.** 창살 좌표를 손으로
재는 대신, 테마끼리 안 변하는 화소를 모으면 그게 곧 유리다. 창틀·창살·벽은 테마색이라
저절로 걸러진다. 8절과 같은 결이다 — **색으로 가르지 말고 불변인 것으로 가른다.**

    유리 자리   x 30.66 ~ 51.69%   y 15.55 ~ 51.07%   (방 그림 대비)
    유리 화소   51,495
    층 비중     하늘 46.5% · 나무 42.3% · 구름 9.6% · 먼 산 1.6%

**단, 창문 조각 안에서만 봐야 한다.** 방 전체로 보면 벽·바닥도 테마마다 변해서
경계상자가 방 전체가 되고, 안 변하는 화소가 창유리 말고도 걸린다.

그리는 쪽에는 마스크가 없다. 그림이 이미 유리 모양으로 잘려 있어서(알파) 방 그림 위에
그대로 얹으면 창틀·창살을 안 덮는다. **창살을 좌표로 잡아 오려내는 코드가 없다.**

### 화면에서 비가 눈이 됐다

11절을 그대로 다시 겪었다. 창문은 실기기에서 폭 101px 이라 원본이 0.43 배로 줄어든다.
길이 7~16px 짜리 빗줄기를 비스듬히 그었더니 **3~7px 점**이 됐고, 점은 눈이다.

세 가지가 다 필요했다. 하나만 고치면 안 됐다.

| | 처음 | 고친 뒤 | 왜 |
| --- | --- | --- | --- |
| 길이 | 7~16px | **27~57px** | 줄어들고도 선으로 남아야 한다 |
| 기울기 | 비스듬 | **0 (수직)** | 기울이면 짧아 보인다 |
| 굵기 | 1~2px | **1px 고정** | 굵은 줄은 유리에 붙은 빗금으로 읽힌다 |

앞뒤 거리는 굵기가 아니라 **밝기 3단**으로 가른다. 굵기로 가르면 굵은 쪽이 다시
빗금이 된다.

### 균등 난수는 고르게 흩어지지 않는다

빗줄기를 `random()` 두 번으로 놓았더니 뭉쳤다. 균등 난수의 성질이 그렇다 — 고르게
퍼지는 게 아니라 **같은 자리에 여러 개가 겹치고 옆이 비는** 쪽이 확률적으로 흔하다.

판을 26x8 칸으로 나눠 **칸마다 한 줄씩** 놓고 칸 안에서만 흔들었다. 눈에 보이는
간격이 곧 칸 크기라 예측 가능해진다.

### 하늘은 세로가 아니라 가로로 균일하다

눈 버전은 잎이 없어야 해서 나무를 지우고 가지를 새로 그린다. 지운 자리를 **같은 x
열의 바로 위 하늘색**으로 메웠더니 세로 줄무늬가 생겼다. 나무 윗변이 열마다 달라서
열마다 다른 높이의 하늘이 내려온 것이다.

하늘 그라데이션은 위아래로 변하고 좌우로는 균일하다. **행 단위로, 그 행의 하늘색
중앙값으로** 채워야 한다.

### 투명을 얹으면 덮이는 게 아니라 뚫린다

밤 하늘에 초승달을 그리려고 채운 원 위에 투명한 원을 얹었다. 달이 **검은 구멍**이
됐다. 알파를 그리는 것은 덮는 게 아니라 **지우는 것**이라 하늘까지 뚫렸다.
보름달과 달무리로 갔다.

### 색만 뽑으면 화풍이 안 따라온다

문밖은 문 비율(0.359)이라 창밖을 늘려 쓸 수 없어서 따로 그렸다. 잎 **색**을 뽑아
매끈한 타원으로 그렸더니 그 부분만 벡터 그림이 됐다. 방 안은 픽셀 아트인데 문 너머만
일러스트다.

색이 아니라 **픽셀 조각을 통째로 떠서** 수관 마스크에 채워야 화풍이 따라온다.
여기서 두 번 틀렸다.

- **나무 경계상자의 왼쪽 위에서 떴다가 크림색 리본이 나왔다.** 거기는 하늘과
  구름이다 — **경계상자의 모서리는 그 물체가 아니다.** 가장 빽빽한 96x96 칸을 찾아
  뜬다
- **그 칸 안의 하늘 틈이 세로 막대로 반복됐다.** 조각을 이어 붙이니 틈도 같이
  반복된 것이다. 칸 안의 비-잎 화소를 잎 화소로 바꿔 순수하게 만든 뒤 쓴다

### 문밖 그림이 코드를 지웠다

문 너머는 원래 **창유리 한 칸을 문 비율로 늘려** 쓰고, 그 위에 잔디·흙길을 코드로
띠 네 개(`band`) 덮어 땅을 만들고 있었다. 창밖만 바꾸면 밤인데 문만 대낮이 되니
어차피 손대야 했는데, 문밖 그림에 하늘·땅·길이 다 들어가면서 걷어낼 게 생겼다.

    DoorSpec.outside                     창유리에서 오려 쓰던 자리
    band(...) 네 줄                      땅을 코드로 덮던 것
    HORIZON · GrassFar · GrassNear ·
    PathSun · PathEdge                   그 띠의 색과 지평선

지평선 기울기(`SHEAR` -12.83%)도 그림에 구워 넣었다. **문 윤곽은 안 구웠다** —
아치와 기울어진 밑변은 그리는 쪽이 `doorPath` 로 오려내므로, 구워 넣으면 윤곽이
두 군데가 되어 반드시 어긋난다.

`lift` 는 남겼다. 띠 코드와 같이 지웠다가 4번(바닥에 번지는 볕)이 그걸 쓰고 있어서
빌드가 깨졌다.

### 재현이 깨져 있던 것 — 파이썬은 실행마다 문자열 해시가 다르다

생성기 씨앗을 `hash((when, weather))` 로 줬다. 돌릴 때마다 다른 그림이 나왔다.
파이썬은 보안상 **프로세스마다 문자열 해시를 무작위로 섞는다.**

`Random("door-night-snow")` 처럼 문자열을 직접 주면 sha512 를 쓰므로 실행과 무관하다.

**커밋 전에 생성기를 다시 돌려 md5 를 대조하는 절차가 이걸 잡았다.** 문밖 6장이 전부
어긋났다. 그 절차가 없었으면 "재현된다"고 적힌 채 재현이 안 되는 스크립트가 들어갔을
것이다. 앞으로도 그림을 넣는 커밋에서는 이걸 한다.

### 낮·밤을 시각으로 자르면 반년은 틀린다

날씨는 **Open-Meteo** 를 직접 부른다. API 키가 없어서 받자마자 전부 돌아간다 —
`local.properties` 에 줄을 더 넣는 방식이면 안 채운 사람은 창밖이 멈춘다. 팀 백엔드를
안 거치는 이유는 `API_BASE_URL` 이 로컬 개발 서버라 같은 와이파이가 아니면 안 닿기
때문이다. **장식용 창문이 서버 상태에 묶이면 대부분의 시간에 기본값만 보인다.**

낮·밤은 시각으로 자르지 않고 응답의 `is_day` 를 쓴다. 한국은 겨울 일몰이 17:15,
여름이 19:57 이라 두 시간 반이 차이 난다. 고정 시각이면 반년은 틀린 그림이 걸린다.

WMO 코드 스물몇 가지를 셋으로 접으면서 두 군데가 갈렸다.

| 코드 | 접는 곳 | 왜 |
| --- | --- | --- |
| 56·57·66·67 어는 비·어는 이슬비 | **눈** | 기상학적으로는 비지만 창밖으로 보이는 것은 하얗게 언 바닥이다 |
| 1·2·3·45·48 흐림·안개 | **맑음** | 그림이 없다. 억지로 비에 넣으면 안 오는 비가 내린다 |

권한은 `ACCESS_COARSE_LOCATION` 만 받고 **측위를 새로 걸지 않는다.** 창밖 그림은
시·군 단위면 충분한데 정밀 위치와 배터리를 장식 하나에 쓸 이유가 없다.

**폴백이 네 갈래를 다 받는다** — 권한 거부 · 위치 없음 · 비행기 모드 · 응답 실패.
전부 기기 시각으로 낮·밤만 잡고 맑음으로 떨어진다. 창문 하나 때문에 오류 문구를
띄우지 않는다. `fetch` 가 실패를 `null` 로 돌리는 것도 같은 이유다 — 거기서 기본값을
만들어 돌려주면 **"못 받았다"와 "맑은 낮이다"가 구별되지 않는다.**

### 확인 수단을 같이 만들어야 했다

여섯 벌 중 앱을 켜서 볼 수 있는 것은 **지금 바깥에 있는 한 벌**뿐이다. 그래서 둘을
같이 넣었다.

- **개발자 패널 칩 여섯.** `outsideOverride ?: liveOutside` — 고르면 실제 날씨를
  이긴다. 실제 시각·날씨가 붙은 뒤에도 이 줄은 남긴다
- **`@Preview` 여섯 벌.** 411dp 폭으로, 문은 0.7 열어 둔다. 안 그러면 문밖을 못 봐서
  절반만 확인하게 된다

그림 고르는 것은 방 안에서 봤다. `IsoMath` 를 코틀린에서 읽어 실제 배치·실제 배율
(`H_STRETCH` 포함)로 그리고, 문 윤곽도 `doorPath` 와 같은 배열로 오려내는 브라우저
페이지다. 원본 크기로만 봤으면 비를 세 번 고칠 일이 없었다 — 원본에서는 처음 것도
비처럼 보였다.

### 남은 것

**실기기 확인이 아직이다.** 에뮬레이터가 두 번 다 안 떴다 — 스냅샷 복귀는 `screencap`
이 빈 버퍼를 돌려주고(화면은 `uiautomator dump` 로 그려지는 게 확인된다), 콜드 부팅은
`adb shell` 이 멈춘다.

**방 안 빛은 안 바꾸기로 했다.** 창밖이 밤인데 방 안이 대낮인 건 그대로 둔다.

    새 파일   12장 · 206KiB (창밖 87 + 문밖 119)
    테스트    75 -> 84

---

## 17. 지도가 이 저장소로 들어옴 (08-29)

처음 역할 분담은 "병원·약국 찾기 지도는 다른 팀원(geo 저장소) 담당, 이 저장소는
홈·미니룸"이었다. 그 분담을 뒤집었다 — **지도 화면의 최종 거처는 이 앱이다.**
사용자가 쓰는 클라이언트는 이 앱 하나고, 지도만 다른 앱에 살 수는 없다.

순서가 중요했다. 백엔드 검색이 먼저 이사를 끝냈다: geo 에서 profile·provider 결합을
끊고(저쪽 #148·#149), 검색 서버를 `SAJOYO/DAENGS_dev` 의 `place-search/` 로 옮겼다
(저쪽 #63, 꺼둔 채 착륙). 그 다음에야 지도를 가져올 수 있었다 — 지도가 가리킬
데이터의 주소가 정해졌으니까.

가져온 것과 안 가져온 것을 갈랐다:

- 가져옴: 네이버 지도 표면 · 현재 위치 · 장소 마커 primitive (geo main@c5f0d5f 에서.
  **이 저장소의 라이브러리 버전을 하나도 안 올리고** 컴파일되는 것을 먼저 실험으로
  확인하고 들여왔다)
- 안 가져옴: 산책 경로선·영토 그리기 (geo 의 연구 영역), 길찾기 UI (백엔드를 안
  옮겼다), Room/추적 (산책 축)
- 다음 카드: 검색 패널·결과 카드, 그 다음 실제 서버 연결 (dev 게이트웨이 확정 후)

지도 진입점은 **임시로 산책기록 탭**이다 — 그 탭은 그동안 아무것도 안 했고, 지도
진입점을 어디 둘지(가운데 발바닥? 새 탭?)는 제품 결정이 나면 옮긴다.

네이버 NCP 키는 카카오 키와 같은 방식이다: `local.properties` 의
`daengs.naverMapClientId`, 없으면 지도 타일만 비고 앱은 켜진다.

---

## 18. 장소 검색 화면의 남은 코드를 옮김 (08-29)

17절에서 다음 카드로 남겼던 **검색 패널·결과 카드**를
`rkbuhtig/DAENGS_geo` `main@c5f0d5f` 에서 이 앱으로 옮겼다. 범위는 그 커밋에 이미
있던 코드로 고정했다:

- 장소 검색 요청·응답 모델·HTTP API·저장소
- 종류 선택, 주차 선호, 결과 카드, 지도 마커 선택, 현재 위치/카메라 중심 재검색
- 장소까지의 길찾기 응답 표시, 네이버 지도 넘기기, 전화 걸기
- 위 코드의 단위 테스트와 서버 응답 JSON fixture

사용자 강아지 등록·선택, 게스트 정책, Fake 저장소, 새 게이트웨이 계약은 만들지
않았다. geo 개발용 강아지 값도 앱 기능으로 옮기지 않고, 원본 계약이 이미 지원하는
강아지 조건 없는 요청을 사용한다.

코드는 들어왔지만 서버 연결까지 끝난 것은 아니다. 장소 API 는 원본 주소인
`/v2/places/search`, 길찾기는 `/journey` 를 호출한다. `SAJOYO/DAENGS_dev` 의
`place-search/` 를 앱의 `daengs.apiBaseUrl` 에서 어느 공개 경로로 받을지는 아직
정해지지 않았으므로, 이 이주에서 임의의 프록시나 새 계약을 만들지 않았다.

## 19. 개발자 도구를 소스셋으로 갈랐다 (08-31)

**되돌리기 번거로운 결정이라 여기 적는다.** `app/src/debug/` 와 `app/src/release/`
에 **같은 이름의 파일이 둘씩** 생겼다. 처음 보면 실수처럼 보이는 모양이다.

출시 준비로 앱을 훑다 릴리스 빌드에 **로그아웃이 아예 없다**는 것을 찾았다.
유일한 로그아웃 버튼이 개발자 패널 안에 있었고 그 패널은 디버그 전용이었다.
회원 탈퇴도 없었다 — 서버엔 `POST /auth/app/withdraw` 가 이미 있는데 앱이 안 불렀다.
마이 탭을 만들어 둘을 옮기고, 그러고 나서 개발자 도구를 릴리스에서 뺐다.

### 왜 플래그가 아니라 소스셋인가

`BuildConfig.DEBUG` 는 **안 보이게 할 뿐 빼지 않는다.** R8 이 꺼져 있어 패널 코드도,
견종 칩 스물일곱 개도 릴리스 APK 안에 그대로 들어갔다. 그리고 그 방식은 이미
어긋나 있었다 — 토글은 감쌌는데 **여섯 줄 아래 패널은 안 감쌌다.**

소스셋으로 가르면 **잊어버리는 순간 빨간 빌드가 되지, 출시되지 않는다.**

대신 규칙이 하나 생겼다. **두 파일의 시그니처가 한 글자도 달라지면 안 된다.**
어긋나면 `assembleRelease` 에서만 깨지므로 확인 절차에 릴리스 빌드가 들어 있다.
그리고 스텁은 **아무것도 그리면 안 된다** — 위 여백을 호출부에 뒀더니 릴리스에서
`Spacer` 만 남아 설명할 수 없는 9dp 가 생겨서, 여백을 도구 안으로 옮겼다.

R8 은 같이 켜지 않았다. 카카오·네이버 SDK 가 얽혀 있어 그 자체로 별도 과제이고,
릴리스에서만 터지는 실패를 부른다.

### 둘러보기도 같은 방법으로 뺐다

출시 앱은 로그인이 필수로 정해졌다. 디버그에는 남긴다 — 방·도감·진단·지도는
인증을 안 쓰는데 개발할 때까지 로그인을 강제하면 서버가 없을 때 아무것도 못 만진다.

**이게 출시 조건을 하나 더 세게 만들었다.** 예전에는 서버가 죽어도 둘러보기로
방까지는 들어갔다. 이제 서버가 안 되면 랜딩에서 한 발도 못 나간다.

### 한 번 되돌린 것

마이 탭에 견종 27종 고르기를 넣었다가 물렸다. 사용자용 고르기는 **"내 강아지 중
대표 고르기"** 여야 하는데 — 어차피 내가 키우는 아이 중에서 고르는 것이다 —
그건 강아지 등록이 있어야 만들 수 있다. 등록이 없는 상태에서 견종을 늘어놓으면
곧 버려질 화면을 만들면서, 사용자에게는 내 개와 무관한 목록에서 하나 고르라는
말이 된다. 그래서 릴리스에서 프로필 얼굴은 당분간 고정이었다 — **20절에서 풀린다.**

## 20. 강아지가 계정에 붙었다 (08-31)

**되돌리기 번거로운 결정이라 여기 적는다.** 강아지 정보를 **기기에 두지 않기로
했다.** 서버가 진짜고 앱은 사본만 들고 있다.

19절 끝에서 "대표 고르기는 등록이 있어야 만들 수 있다"고 적어 둔 그 등록이다.
`SAJOYO/DAENGS_dev` 에 `pets` 테이블과 `/app/pets/*` 를 먼저 올리고(PR #88),
그 다음에 앱을 붙였다. 순서를 반대로 했으면 기기에 저장하는 코드를 짰다가
서버가 생긴 뒤 다시 걷어냈을 것이다.

### 대표는 강아지가 아니라 계정이 들고 있다

`pets.is_primary` 로 두면 **두 마리가 동시에 대표**인 상태를 막으려고 부분 유니크
인덱스가 필요하다. `app_users.primary_pet_id` 로 두면 DB 가 한 마리만 대표인 걸
저절로 보장하고, 대표를 바꾸는 것도 UPDATE 한 번이다.

대표를 지우면 남은 것 중 **가장 먼저 등록한 아이로 자동 승계**한다. "대표가 없는
상태"를 안 만들면 화면이 단순해진다. 첫 아이는 등록과 동시에 대표가 된다.

**그래서 앱은 무엇을 바꾸든 목록을 다시 받아 온다.** 승계는 서버가 말없이 하는
일이라, 응답 하나만 보고 앱이 상태를 짐작하면 어긋난다. 그 규칙을 앱에서 다시
계산하면 두 벌이 되고 언젠가 갈라진다.

### 모르는 것은 모른다고 저장한다

생일·중성화·몸무게를 **필수로 하지 않았다.** 필수로 하면 모르는 사람이 아무 날짜나
넣고, 그러면 그 값은 데이터로 못 쓴다. 중성화는 예·아니오·**모름** 세 상태다
(`PlaceFacts` 가 "모름을 false 로 바꾸지 않는다"고 적어 둔 원칙과 같다).

생일은 **날짜 하나 + 어느 쪽인지**를 같이 저장한다. 모르면 "가족이 된 날"로
대체하는데, 한 칸에 넣으면 "3살"과 "함께한 지 2년"을 구분할 수 없다.
DB 가 `CHECK ((birth_date IS NULL) = (birth_date_kind IS NULL))` 로 둘이 같이
있거나 같이 없는 것만 받는다.

### 견종은 문자열이다

서버는 견종 어휘를 검사하지 않는다. 그림이 있는 27종은 우리 쪽 목록이고, 저쪽에
그 목록을 박아 두면 그림이 늘 때마다 서버를 같이 고쳐야 한다. 그래서 앱이 받은
문자열을 그대로 보내고, **화면에서 그림을 찾을 때만** `DogBreed.byId` 로 맞춰 본다.
못 찾으면 발자국 얼굴로 떨어진다 — 믹스가 그 자리다. 아무 얼굴이나 골라 보여 주면
사용자는 자기 개가 아닌 얼굴을 보게 된다.

### 실기기에서 걸린 것

앱을 켜 두고 몇 분 뒤에 강아지를 등록하니 **"인증이 만료되었습니다"** 가 떴다.
access 토큰이 5분인데 강아지 호출이 들고 있던 세션 토큰을 그대로 썼다. 탈퇴에는
재발급 사다리가 있었는데 여기엔 없었다. `freshToken()` 한 군데로 모으고 탈퇴도
그걸 쓰게 했다 — 재발급이 되면 **세션도 같이 갈아 끼워야** 다음 호출이 또 만료를
안 만난다.

그리고 `PetSection` 을 만들어 놓고 **화면에서 부르는 줄을 빠뜨렸다.** 파이썬으로
문자열을 바꿔치기하면서 바뀌었는지 확인을 안 걸어 조용히 실패했다.

### 마릿수 5마리는 아직 임의값이다

정하는 기준은 "몇 마리가 자연스러운가"가 아니라 **방에 몇 마리까지 담기나**다.
12x12 격자에 가구가 차 있고 `MIN_DOG_GAP` 이 1.3칸이라, 실기기에서 5·8·10 을
세워 보고 정해야 한다. 등록한 강아지와 방에 도는 강아지는 아직 별개다.

## 21. 산책 기록을 기능 경계 다섯 개로 옮김 (08-31)

`rkbuhtig/DAENGS_geo@a145375927ce2372de376475f85e1377eeaf0168`의 산책 기능을 이 앱으로
옮겼다. 백엔드까지 통째로 가져오지 않고, 각 단계가 독립적으로 검증되도록 다섯 PR로
나눴다.

1. 위치 표본을 흔들림·저정확도·비현실적 점프 기준으로 정리하는 순수 `TrailRecorder`
2. 화면 필터 전에 원본 fix를 직렬로 저장하는 Room 로그
3. 서로 끊긴 세그먼트를 직선으로 잇지 않는 지도 레이어와 네이버 경로선
4. 화면 밖에서도 기록을 유지하는 location Foreground Service와 알림 제어
5. 산책기록 탭의 시작·일시정지·재개·종료, 실제 활동 시간·거리와 지도 연결

**원본과 화면용 경로를 일부러 갈랐다.** 표시선은 GPS 흔들림을 버려야 깔끔하지만,
그 문턱값은 나중에 바뀔 수 있다. 버린 점을 복구할 수 있도록 기기가 준 원본은 먼저
Room에 쓰고, 화면에는 필터 결과만 보낸다. 일시정지와 GPS 점프 뒤에는 새 세그먼트를
만들어 걷지 않은 공간을 직선이나 거리로 세지 않는다.

서비스는 `START_NOT_STICKY`다. 프로세스가 죽었다고 앱이 사용자 동의 없이 산책을 다시
시작하지 않는다. 명시적인 종료만 세션을 닫고, 강제 종료된 세션은 Room에 열린 상태로
남긴다. 데이터는 보존되지만 이어 기록/폐기 UI는 후속 과제다.

가져오지 않은 것도 명확하다. 서버 업로드·점수·영토·리플레이·내보내기는 이 앱의 순수
기록 흐름에 필요하지 않아 제외했다. 홈의 `오늘의 산책 요약` 숫자도 아직 데모다. 이번
작업이 보장하는 범위는 **로컬 원본 저장, 화면 밖 기록, 현재 산책의 시간·거리·경로**까지다.

## 26. 산책 원본 업로드와 계산 완료를 나눴다 (09-02)

예전 앱은 마지막 좌표 chunk가 서버에 들어가면 `syncedAtMillis`를 찍고 그 산책을 다시
보지 않았다. 그런데 서버의 산책 분석은 별도 finalize에서 만들어진다. **원본이 있는
것과 계산 결과가 있는 것은 다른 사실**인데 한 칸으로 표현해서, 업로드된 산책이 계산
없이 영원히 남을 수 있었다.

그래서 로컬 상태를 `local_only → raw_uploaded → derived`로 나눴다. 모든 chunk가 성공한
뒤 서버 walk id와 `raw_uploaded`를 먼저 저장하고, 좌표 수와 마지막 `client_seq`를 담은
manifest로 finalize한다. finalize 응답을 잃으면 저장한 id로 **finalize만** 다시 부른다.
원본을 매번 다시 보내지 않지만, 두 API 모두 멱등이라 어느 경계에서 앱이 종료돼도
결과는 한 벌이다.

### 예전 synced 기록은 derived가 아니다

Room 4→5 마이그레이션에서 `syncedAtMillis`가 있던 행은 `derived`가 아니라
`raw_uploaded`로 옮긴다. 그 시각은 옛 코드에서 chunk 업로드 직후 찍혔기 때문이다.
서버 walk id는 예전 DB에 없으므로 한 번은 `client_session_id`로 create를 다시 불러 id를
얻는다. create와 append가 멱등이라 이미 있던 좌표는 늘어나지 않고, 그 다음 finalize로
계산을 보충한다. 이 기록을 `derived`로 옮기면 분석이 없는 과거 산책을 복구할 길이
사라진다.

서버에서 되찾은 산책도 목록 응답에 분석 상태가 없으므로 `derived`라고 추측하지 않는다.
서버 id를 아는 `raw_uploaded`로 저장하고 다음 동기화에서 finalize한다. 이미 계산된
산책이면 서버가 같은 analysis를 돌려주므로 결과는 바뀌지 않는다.

## 27. 카드 글자를 저쪽 대신 우리가 지운다 (09-03)

저쪽이 준 열 장은 강아지 이름 자리를 **모자이크로 뭉개** 놓았다. 자국이 그대로 보여서
`tools/name_slot.py` 로 그 블록을 찾아 메웠는데, 지우다 만 티가 남았다. 번호 자리는
아예 손을 못 대서 열 장이 `code = null` 이었고, **생일이 안 나오는 대신 저쪽 개 이름
`NEO-0824` 가 인쇄된 채로 남아 있었다.**

**역할을 바꿨다. 저쪽은 얼굴만 뚫고, 지우는 것은 우리가 한다.** 되돌리기 번거로운
결정이라 여기 적는다 — 앞으로 받는 원화는 **글자가 인쇄된 채**여야 한다. 뭉개서 오면
지울 때 참고할 바탕이 사라져서 오히려 어려워진다.

**직사각형을 통째로 메우면 안 된다.** 예전 두 도구(`erase_text` · `smear`)는 글자 자리를
사각형으로 잡아 좌우 이웃 색으로 채웠다. 이 카드에서는 그러면 제목 바의 **대각선
잘림과 은색 베벨이 같이 날아간다.** 글자 화소만 마스크로 잡아 그 자리만 잇는다.

**줄무늬는 양 끝을 한 화소로 읽어서 생겼다.** 줄마다 `왼쪽 한 점 → 오른쪽 한 점` 을
이었더니 그 점에 실린 잡음이 줄마다 다른 기울기를 만들어, 순검정 바 위에 가로 띠가
깔렸다. 끝 14화소의 **가운뎃값**을 쓰니 사라졌다.

**지우는 범위를 줄이면 더 나빠진다.** "흔들려 보인다" 는 말에 반경을 줄여 봤는데
글자 조각이 점선으로 남았다. 남은 것은 덜 지운 후광이지 과하게 지운 자국이 아니었다.

**번호판 여덟 장은 복원을 포기했다.** 홀로그램 무지개 위라 30px 안에서 색이
`(230,219,51)` → `(234,255,254)` 로 튄다. 가로로 이으면 노란 띠, 세로로 이으면 줄무늬,
아래를 복제하면 이음매다. 대신 앱이 **어두운 칩**을 깔아 덮는다 — 생김새는 당근형
넷에 저쪽이 인쇄해 둔 검은 상자에서 그대로 뽑았다.

**이름은 바 전체를 쓴다.** 저쪽 제목은 `MUSHROOM NEO` 처럼 야채 + 개 이름이라 예전에는
뒷말만 갈아 끼웠다. 바가 거의 단색이라 통째로 지우는 쪽이 오히려 깨끗하고, 그리드
크기에서도 읽힌다. 야채 이름은 도감 칸에 남는다.

**글씨체를 하이커(HiKR) ExtraBold 로 바꿨다.** 케듀는 획 끝이 둥글어 각진 원화 옆에서
물러 보였다. OTF 가 391KB 로 케듀(438KB)보다 작아 APK 는 오히려 줄었다. 라이선스는
임베딩 허용이지만 **폰트 파일 재배포는 허용이 아니다** — 파일을 따로 빼거나 서브셋으로
잘라 넣지 말 것.

**배추·고구마도 같은 캔버스로 갈아 끼웠다.** 열두 장이 1080×1440 하나가 됐고, 캔버스가
바뀌었으니 구멍을 다시 쟀다. 저쪽 `measure_holes.py` 값과 0.6%p 안에서 맞는다.

## 28. 산책 순간은 묶음이 아니라 누른 행동을 남긴다 (09-03)

산책 화면의 탐색·교류·특별 버튼은 처음에는 실행 중 메모리에만 있었다. 끝난 산책의
경로를 다시 읽어도 순간은 사라졌고, 앱 프로세스가 죽으면 완료 화면에서도 복원할 수
없었다. Room을 v6으로 올려 `walk_action`에 **버튼 한 번의 종류·누른 시각·그때 사용한
GPS fix**를 저장한다. 세션을 지우면 외래 키 `CASCADE`로 같이 지워져, 강아지 삭제나
회원 탈퇴 뒤에 생활권 좌표만 남는 일도 막는다.

**5m로 합친 장소는 저장하지 않는다.** 묶음은 표현 규칙이고 버튼 입력은 원본이다.
묶음을 저장하면 나중에 반경을 조정하거나 표시 방법을 바꿀 때 과거 기록을 되돌릴 수
없다. 그래서 상세를 읽을 때 행동 원본을 시각 순으로 정렬하고 현재 규칙으로 다시
묶는다. 같은 종류를 같은 장소에서 여러 번 누르면 기존 입력 계약대로 최초 기록 하나만
받고, 서로 다른 종류의 입력은 각각 보존한다.

화면의 마커는 누르는 즉시 반응하지만 **저장했다는 문구는 Room 쓰기가 끝난 뒤에만**
보낸다. 쓰기 큐에서 하나라도 실패하면 종료 시 `flush`도 실패하므로, 저장되지 않은 순간을
보여 준 채 산책 전체가 정상 완료된 것처럼 넘어가지 않는다.

행동 종류는 문자열 코드로 저장한다. 미래 앱이 새 종류를 남긴 DB를 예전 앱이 읽으면
그 행만 건너뛰고 세션과 경로는 계속 연다. v5→v6 마이그레이션은 기존 세션·강아지·좌표·
동기화 상태를 그대로 보존하며, 예전 산책에 없던 행동을 경로에서 추측해 만들지는 않는다.

서버 업로드에는 넣지 않았다. 현재 산책 API에는 행동 계약이 없어서 좌표 chunk나
finalize payload에 몰래 섞으면 서버와 앱 중 한쪽만 이해하는 데이터가 된다. 서버 계약이
정해지기 전까지 순간 기록은 이 기기의 상세 화면에서만 복원한다.

## 29. 에뮬레이터 산책을 실제 측정과 갈랐다 (09-03)

산책 fix는 처음부터 `isMock`을 Room과 서버 계약까지 운반했지만, 출발점은 Android의
`LocationCompat.isMock` 하나였다. 일부 Android Studio AVD는 `adb emu geo fix`로 넣은
좌표에도 이 표식을 붙이지 않는다. 그러면 검증용 경로가 `device` 증거로 서버에 남아
거리·속도·정지 문턱을 고르는 실측 표본과 섞인다.

플랫폼 표식이 있으면 항상 mock으로 보고, 빠졌을 때만 fingerprint·model·manufacturer·
device·product의 에뮬레이터 특징을 함께 본다. `Google`이라는 제조사 이름만으로 판단하지
않는다. 실제 Pixel도 같은 제조사이므로 그것까지 막으면 정상 사용자의 위치가 사라진다.

이 판정은 산책 전용 보정이 아니라 `LocationSample`의 출처 계약이다. 장소 검색 같은 다른
소비자가 개발 중 mock 좌표를 허용해야 한다면 그 소비자 정책에서 명시적으로 열고, 공용
위치 표식을 실제 좌표인 것처럼 바꾸지 않는다. 판정 경계는
`rkbuhtig/DAENGS_geo@2b485ec`에서 가져왔고, App의 마지막 위치 fallback은 유지했다.

## 지금 남아 있는 것

[STATUS.md 의 "다음 결정"](STATUS.md#다음-결정) 을 보면 된다. 큰 것만 적으면
**산책 게임**(기록까지는 21절에서 들어왔다), **등록한 강아지를 방 안에 세우는 것**
(여기서 5마리 상한도 같이 정해진다), 소품 해금 규칙이다.

## 22. 5마리는 재서 정한 숫자다 (08-31)

**되돌리기 번거로운 결정이라 여기 적는다.** 한 계정에 강아지 5마리는 눈대중이 아니라
측정에서 나왔다. 다음에 이 숫자를 흔들 사람이 **무엇을 다시 재야 하는지** 알게 적는다.

20절에서 강아지를 계정에 붙일 때 상한을 5로 두면서 "임의값"이라고 적어 뒀다.
정하는 기준은 "몇 마리가 자연스러운가"가 아니다 — **등록한 강아지가 방에 서기
때문에**, 12x12 격자에 가구가 찬 방에 몇 마리까지 담기나가 기준이다.

### 먼저 방이 등록한 강아지를 따라가게 했다

미니룸 강아지는 `ROOM_BREEDS` 세 종을 돌려 쓰는 장식이었다. 상단바 얼굴은 대표를
따르게 해 놓고 방만 안 따라서, **같은 화면이 두 말을 하고 있었다.**

그림을 새로 받을 필요는 없었다. **27종 전부 걷기 시트가 있다** — `ROOM_BREEDS` 가
셋인 것은 아트 한계가 아니라 MVP 선택이었다. 아트가 없어서 못 하는 줄 알고 미뤄
뒀는데, 세어 보니 아니었다.

`DogHerd` 를 마리 수가 아니라 **명부**로 만들었다. 딸려온 것 둘.

- **자리를 지킨다.** 목록은 등록·수정·대표 변경 때마다 다시 받아 오는데, 그때 방을
  새로 세우면 걷던 아이들이 순간이동한다. 남는 마리는 그대로 두고 견종만 간다
- **개발자 도구를 되돌리면 내 강아지로 온다.** `setBreedOverride(null)` 이
  `ROOM_BREEDS` 로 돌아가고 있었다. 그대로 뒀으면 도구를 껐을 때 방에 남의 개가 남는다

### 몸 반경으로 재면 12마리도 "괜찮다"고 나온다

처음엔 `bodyRadius` 로 겹침을 쟀다. 12마리에서도 겹치는 비율이 0.1% 라 상한을 둘
이유가 없다는 결론이 나왔는데, **화면은 이미 뭉쳐 보인다.**

그림 폭이 몸 반경보다 훨씬 넓어서다. 견종 폭 13% 는 격자로 1.56칸인데 `MIN_DOG_GAP`
은 1.3칸이다 — **목적지를 규칙대로 벌려도 그림은 겹친다.** 그래서 재는 자를 스프라이트
폭으로 바꿨다.

```
마리   겹치는 쌍 평균   겹치는 프레임 %
  4        0.27             24.3     <- 예전 고정값
  5        0.50             42.4     <- 상한
  7        0.82             53.5
  8        1.15             72.8     <- 여기서 1을 넘는다
 10        2.35             95.3
```

**8마리에서 1을 넘는다** — 언제 봐도 어딘가 두 마리가 겹쳐 있다는 뜻이다. 5마리는
예전 4마리의 두 배지만 아직 "가끔"이라 **5로 뒀다.** 늘린다면 7이 한계고, 8부터는
방을 넓히거나 마리당 자리를 다시 봐야 한다. 서버 `MAX_PETS_PER_USER` 도 5 그대로다.

`DogCrowdingTest` 가 이 표와 함께 남아서, 방이 좁아지거나 강아지가 커지면 깨진다.
대조군(상한 두 배는 뭉친다)도 같이 둬서 **방을 안 고치고 상한만 올리면** 잡힌다.

### 믹스는 방에서만 대역을 세운다

얼굴은 발자국으로 떨어뜨렸지만 방에서는 그럴 수 없다 — 걷고 앉는 전신 시트라 대체할
중립 그림이 없고, 안 세우면 **자기 강아지가 방에서 사라진다.** 강아지 id 로 대역을
정해서 앱을 껐다 켜도 같은 종이다. 믹스 시트가 오면 `roomStandIn` 만 바꾸면 된다.

## 23. 산책에 강아지가 여러 마리 붙는다 (08-31)

**되돌리기 번거로운 결정이라 여기 적는다.** 로컬 Room 과 서버 양쪽에서 컬럼 하나를
표 하나로 바꿨다. 데이터가 이미 쌓인 뒤에 한 일이라 되돌리려면 또 옮겨야 한다.

산책은 `walk_session.dogId` · `walks.pet_id` 한 칸에 강아지 하나만 붙었다. 두 마리를
데리고 나가는 사람에게는 **한 아이의 기록만 남는다.** 나중에 챗봇이 "이 아이 이번 주
운동량"을 말하려면 그때 그 아이가 나갔는지가 남아 있어야 하는데, 그 구조로는 나머지
아이의 산책이 통째로 빈다.

### 컬럼을 지우려면 표를 다시 만들어야 했다

minSdk 26 의 SQLite 에는 `ALTER TABLE ... DROP COLUMN` 이 없다 (3.35 부터다).
그래서 마이그레이션이 네 걸음이다 — **값을 먼저 옮기고**, 새 표를 만들어 옮겨 담고,
옛 표를 지우고, 이름을 바꾼다. 순서를 바꾸면 아직 서버에 사본이 없는 산책이 사라진다.

좌표(`walk_fix`)는 손대지 않는다. 이름으로 참조하므로 새 표가 같은 이름을 달면 그대로
이어지고, Room 이 마이그레이션 동안 외래키를 꺼 두기 때문에 `DROP TABLE` 이 좌표를
쓸어 가지도 않는다. **이 두 가지를 믿고 짠 코드라** `WalkMigrationTest` 가 진짜 SQLite
파일로 그걸 확인한다 — 믿음이 깨지면 사용자의 산책이 날아가는 종류의 일이다.

### 삭제 규칙의 뜻은 그대로 두었다

서버에서 단수 `pet_id` 는 `ON DELETE SET NULL` 이었다. 무지개다리를 건넌 아이와의
산책이 그 아이를 지웠다고 없던 일이 되면 안 되니까. 조인 행은 `CASCADE` 지만 **뜻은
같다**: 강아지를 지우면 그 연결만 사라지고 산책 자체는 남는다. 조인 행에 NULL 을
남기면 "누군지 모를 아이"라는 뜻 없는 줄이 쌓인다.

### 이름은 기록에 안 넣는다

목록에 "네옹 · 댕댕"이 뜨지만 그 이름은 **저장된 값이 아니다.** 지금의 강아지 목록에서
id 로 찾는다. 이름은 바뀌는 값이라 기록에 박아 두면 개명한 뒤에도 옛 이름이 뜬다.

대가가 있다 — 강아지를 지우면 그 산책은 "누구와 갔는지 모르는" 상태가 된다.
**연결 줄은 남으므로** 그 아이가 서버에서 다시 내려오면 이름도 다시 붙는다. 그 자리를
"알 수 없는 아이"로 채우지 않는 것도 같은 이유다. 없는 사실을 만드는 것보다 비는 편이
낫다 (`PlaceFacts` 와 같은 원칙).

### 처음 열 때만 아이를 붙인다

`insertSession` 이 rowId 를 돌려주게 해서, **이미 있는 세션이면 아이를 안 붙인다.**
안 그러면 재시도 한 번에 그날 데리고 나가지 않은 아이가 그 산책에 섞인다. 시작 시각을
안 덮어쓰는 것과 같은 판단이다.

## 24. 카메라를 앱 안으로 (09-01)

**되돌리기 번거로운 결정이라 여기 적는다.** 시스템 카메라로 던지던 것을 앱 안 카메라
(CameraX)로 바꿨다. 의존성 넷과 권한 하나가 늘었다.

피부 사진은 병변이 화면 가로의 28~68% 로 찍혀야 서버가 추론하고, 보행 영상은 뒤에서
전신이 들어와야 한다. 그런데 **찍는 화면이 우리 것이 아니라서 그 안내를 올릴 자리가
없었다.** 첨부한 뒤 네모를 옮길 수는 있었지만 그건 이미 찍힌 사진 안에서 하는 일이다.

### 거부하면 시스템 카메라로 떨어질 수 **없다**

처음에는 "권한을 거부하면 지금까지의 시스템 카메라로 떨어지게" 만들었다. 실기기에서
권한을 끄고 눌러 보니 **앱이 죽었다.**

```
java.lang.SecurityException: Permission Denial: starting Intent
{ act=android.media.action.IMAGE_CAPTURE } with revoked permission android.permission.CAMERA
```

매니페스트에 `CAMERA` 를 선언한 앱은 그 권한이 없으면 **시스템 카메라 인텐트조차 못
띄운다.** 선언하지 않은 앱은 자유롭게 띄울 수 있는데, 선언하는 순간 안드로이드가 그
권한으로 문을 잠근다. 즉 앱 안 카메라를 넣는 순간 "권한 없이 시스템 카메라로" 라는
길은 **사라진다.**

그래서 거부했을 때는 찍지 못하고 그렇게 말한다 — "설정에서 켜거나, 갤러리에서 골라
주세요". 갤러리는 권한 없이 되므로 할 수 있는 일이 남는다.

**되던 일을 막지 않는다는 원칙은 지키되, 무엇이 가능한지를 먼저 확인해야 했다.**
원칙만 믿고 코드를 쓰면 이렇게 죽는다 — 실기기에서 눌러 보고서야 알았다.

### 화질은 기본값이 아니라 우리가 정해야 했다

`LifecycleCameraController` 의 기본은 지연 최소화에 화면 크기 해상도다. 1200만 화소
카메라에서 시스템 카메라의 절반도 안 되는 사진이 나왔다. **병변을 보는 사진이라
화질이 곧 판정이다** — 최대 품질·가장 큰 4:3 으로 바꿨다.

⚠️ **미리보기까지 최대 해상도를 요구하면 카메라가 안 열린다.** `onDeviceError` 로
닫히고 사진이 안 찍혔다. 미리보기는 비율만 맞추고 크기는 맡긴다.

### 화면에서 본 자리가 곧 찍히는 자리여야 한다

프리뷰를 화면 전체에 채웠더니(FILL_CENTER) 4:3 사진이 잘려서, 가이드 네모에 맞춰
찍었는데 확인 화면에서는 네모가 딴 데 있었다. **프리뷰 상자를 사진과 같은 3:4 로
잡아** 잘릴 것을 없앴다.

그리고 맞춰 찍었으면 **확인 화면이 그 네모에서 시작한다.** 맞춰 찍고 또 맞추게 하면
가이드가 소용없었다는 뜻이 된다.

### 가이드는 판정하지 않는다

시안에는 가이드가 초록/빨강으로 바뀌는 그림이 있었다. 그건 강아지가 제자리에 있는지를
센서로 재야 하는 일이고 그 판정이 없다. 없는 판정을 색으로 흉내 내면 **"초록이었는데
왜 다시 찍으라느냐"** 가 된다. 보행 화면이 먼저 그렇게 정해 뒀고, 피부 쪽도 같게 뒀다.

네모 크기는 여기서 정하지 않는다 — 서버가 실측한 권장 밴드의 한가운데다. 찍을 때의
안내와 찍고 나서의 판정이 다른 숫자를 쓰면 "가이드에 맞췄는데 너무 작아요" 가 뜬다.

## 25. 챗봇 자유 텍스트가 오케스트레이션으로 갔다 (09-01)

`ChatScreen` 의 자유 텍스트 전송이 `POST /assistant/query`(`SAJOYO/DAENGS_dev`)로
간다. 이번 카드에서 **로컬 키워드 라우팅(`GAIT_ASK` 정규식)을 지웠다** — 서버 의미
라우터가 자연어 해석을 전담하는 게 오케스트레이션의 요지라, 앱이 먼저 텍스트를
보고 갈래를 나누면 그 요지와 어긋난다. 대신 화면은 서버가 준 `status`·`handoffs` 만
보고 무엇을 할지 정한다(`ui/chat/AssistantChat.kt` 의 `bubbleMessage`·`knownHandoff`).

**대화 기록을 안 보낸다.** v1 오케스트레이션이 상태가 없어서 매 전송이 독립 질의다 —
앱에서 먼저 메모리를 흉내 내면 서버가 나중에 진짜로 상태를 가질 때 둘이 갈라진다.

**읽기 타임아웃을 60초로 뒀다.** 배포 스모크에서 Training 질의가 약 11.5초 걸려
성공했는데, `AuthApi` 의 10초 타임아웃을 그대로 썼으면 성공한 요청을 실패로 봤을
것이다.

**HANDOFF 는 새 화면을 안 만든다.** gait 는 기존 `ChatEntry.GaitIntro` 카드를 그대로
얹고, skin 은 기존 `AiActionDialog` 를 재사용하되 `skinOnly` 플래그로 보행 묶음만
감춘다 — 그 문답은 이미 피부 얘기였으니 보행 선택지를 보여줄 이유가 없다. 두 경우
다 능력을 앱이 실행하지 않는다. 실행은 사용자가 사진/영상을 실제로 골랐을 때
기존 `ScreeningApi`/`GaitHolder` 가 한다.

**카드를 앱 안 카메라(24절) 위에 얹었다.** skin HANDOFF 로 연 선택 시트에서 카메라를
고르면 위 24절의 `withCamera` 권한 흐름 → CameraX 촬영 화면으로 그대로 이어진다 —
오케스트레이션은 "어느 화면을 열지"만 정하고, 그 화면 자체는 그대로 재사용한다.

## 26. 산책 판정만 `results` 를 읽는다 (09-02)

**되돌리기 번거로운 결정이라 여기 적는다.** 25절에서 `AssistantResponse` 는
`results`(능력별 실행 결과)를 **일부러 안 옮겼다** — "저쪽 결정론적 집계를 앱에서
두 번째로 만들면 둘이 갈라진다" 였다. 그 원칙에 산책 하나만 구멍을 냈다.

실기기에서 산책 날씨를 물었더니 답이 `현재 산책 판단: GOOD` 한 줄이었다. 등급만
있고 왜 그런지도, 언제 나가면 좋은지도 없었다. 저쪽을 따라가 보니 데이터가 없는 게
아니었다. `orchestration/aggregate.py` 의 `_result_message()` 는 능력이 준
`data["answer"]` 문자열을 그대로 통과시키는데 — **훈련·생활은 완성된 한국어 답변이
거기 들어 있고, 산책만 그 키가 없다.** 구조화된 판정이라서다. 그래서 그 자리에서
등급 하나로 문장을 지어내고 나머지를 버린다.

버려지는 것이 `WalkOut` 전체다. 체감온도·미세먼지·강수의 축별 한 줄(`axes[*].note`),
등급을 깎은 축(`dominant`), 시간별 등급 24개, 추천 산책 시간대(`windows`), 출처
표기(`location.label`). **이미 앱까지 오고 있었다** — 저쪽이 `results` 를 필터 없이
반환하므로 우리가 안 읽고 버리던 것뿐이다.

**두 갈래였다.** (가) 저쪽에서 문장을 더 잘 짓게 한다. (나) 앱이 `results` 를 읽어
카드로 그린다. (나) 로 갔다. 담을 것이 산문이 아니라 측정값과 시간 범위라 문장으로
뭉개면 시간대를 살릴 방법이 없고, 무엇보다 **위험한 날에 색을 줄 수가 없다** —
저쪽은 `UNSAFE` 도 `CapabilityStatus.OK` → 최상위 `ANSWERED` 로 보내서, 문장만으로는
평범한 답변과 구분이 안 된다.

**이건 집계를 두 번 하는 게 아니다.** 등급을 매기는 것도, 어느 축이 깎았는지도,
시간대를 고르는 것도 여전히 저쪽이 한다. `WalkVerdict` 는 이미 나온 결론을 읽어
자리에 놓을 뿐이고 계산하는 값이 하나도 없다. 이유 줄도 저쪽이 지은 문장을 그대로
쓴다 — 우리가 다시 쓰면 같은 값을 두 군데서 말하게 된다.

**대가는 저쪽 스키마와 묶인 것이다.** 그래서 `WalkVerdict.parse` 는 모르는 등급을
`UNKNOWN` 으로 떨어뜨리고, 시각이 안 읽히는 시간대는 그 줄만 버리고, `now` 가 통째로
없어도 안 죽는다 (`Status` 에 `UNKNOWN` 을 둔 것과 같은 이유). 저쪽이 필드를 바꾸면
카드가 비는 정도로 끝나야지 크래시가 나면 안 된다.

**등급 문장은 카드가 아니라 말풍선이 맡는다.** 여긴 챗봇이라 판정은 말로 오는 편이
자연스럽고, 카드까지 같은 등급을 크게 적으면 두 번 읽게 된다. 그래서 산책만 물었을
때는 저쪽 한 줄을 대화체 문장으로 갈음한다(`walkSentence`). **능력이 둘 이상이면
갈음하지 않는다** — 그때 `message` 는 `[산책]…[훈련]…` 으로 이어붙은 것이라, 갈아
끼우면 산책이 아닌 답변까지 사라진다.

**카드는 접힌 채로 온다.** 대화는 흘러가는 곳이라 매번 다 펼쳐져 있으면 위에 있던
이야기가 밀려난다(비교 결과를 접어 둔 것과 같은 이유). 접혔을 때도 추천 시간대는
보인다 — 이 응답에서 바로 쓸 수 있는 건 그것뿐이다.

**축은 등급을 앞에, 숫자를 뒤에 둔다.** 처음에는 `axes[*].note` 만 꺼내 `PM10 42㎍/㎥`
를 그대로 줄로 세웠는데, 실기기에서 보니 그 숫자로 판단할 수 있는 사람이 많지 않다.
그런데 **해석은 이미 저쪽이 해놨다** — `axes` 안에 축마다 등급이 따로 있다. 그래서
`미세먼지 좋음 PM10 42㎍/㎥` 순으로 세운다. 등급이 우리 판단이 아니라 서버 값이라
기준이 갈라지지 않고, 숫자는 아는 사람만 보면 된다. `heat` 를 "더위" 로 안 적는다 —
그 축은 한파도 같이 보는 자리라 겨울에 틀린 말이 된다.

**`dominant` 는 안 쓰고 축 셋을 항상 보여준다.** 등급을 깎은 축만 보여주면 좋은 날에
줄 수가 들쭉날쭉하고 "비는 어떤데?" 를 알 수가 없다. 어느 축이 문제인지는 등급
단어가 이미 말한다. 순서도 저쪽 JSON 키 순서를 안 따른다 — 약속된 것이 아니라
같은 카드가 볼 때마다 다르게 보인다.

**`notes` 는 안 옮긴다.** 저쪽 `_notes()` 는 "사용자가 알아야 할 한계" 라고 적혀
있지만 실제 문구는 `"대기질 미래 구간은 권역 일 예보 기준이다 (③-b — 관측값의 지속
가정은 기각했다)"` 처럼 **명세 절 번호가 박힌 방법론 각주**다. 판정을 어떻게 냈는지의
기록이지 나갈지 말지 정하는 데 쓸 말이 아니다. 그중 사용자가 쓸 수 있는 것은 이미
다른 자리에 있다 — 모르는 축 때문에 깎인 것은 `capped`, 어느 측정소인지는
`location.label`.

**시간별 등급(`timeline`)을 카드에 편다.** 실기기에서 "3시간 후에는?" 을 물었더니
같은 답이 왔다. 무상태라 그 질문이 앞 문답을 모르는 새 질문으로 가서 서버가 다시
"지금" 을 판정한 것이다. 앞으로 24시간 값이 이미 응답에 들어 있으니 펴 두면 되묻지
않아도 된다. **되묻기 자체를 없애는 건 아니다** — 문맥은 여전히 저쪽 v2 몫이다.

**`windows` 의 `to` 는 구간의 끝이 아니다.** "마지막으로 좋은 시각" 이다(그다음까지
좋다고 하면 없는 것을 지어내는 것이라 저쪽이 그렇게 준다). 좋은 시점이 하나뿐이면
`from` 과 같아지는데, 그걸 범위로 그려서 실기기에 `12시~12시` 가 찍혔다. 같으면
한 시점으로 쓴다.

**등급을 모를 때(`ABSTAINED`)는 카드를 안 띄운다.** 그건 판정이 아니라 못 하겠다는
말이라 등급 칸과 시간대 칸에 담을 것이 없다. 저쪽 문장을 그대로 말풍선으로 보여준다.

`training`·`life` 의 `citations`(문서 제목·URL)도 같은 이유로 아직 안 읽고 있다.
답변에 `[1]` `[2]` 로만 보이는 그 인용의 원본이다 — 필요해지면 여기와 같은 방식으로
연다.

## 30. 과일 한 벌이 들어오면서 "카드는 한 판" 이라는 전제가 깨졌다 (09-04)

과일 열세 장을 받았다. 사과·바나나·블루베리·체리·망고·멜론·복숭아·배·딸기·수박 열 장에
**키위 에디션 셋**(`FUZZY PORTAL` · `SEED SPARK` · `GENTLE MONSTER`)이다. 도감은
야채·과일 탭으로 갈리고 **뽑기는 스물다섯 종 균등**이다 — 갈래를 안 가린다.

되돌리기 번거로운 결정 넷을 여기 적는다.

**갈래를 `DexDeck` enum 으로 뒀다.** `type` 문자열로 가르지 않는다. 그건 설명 시트의
`Type` 줄에 그대로 표시되는 값이라 표시용과 분류용이 엉킨다. 실제로 과일 안에도
`FRUIT DOG` 와 `FRUIT COLLECTION` 두 가지가 있어서 문자열로는 못 가른다.

**번호를 벌마다 1부터 매겼다.** 야채 01~12, 과일 01~13. 그래서 `IMMERSIVE_SCENES` 의
키를 `no` 에서 `id` 로 바꿨다 — 번호를 키로 두면 **과일 1번(사과)에 배추 무대가 붙는다.**
곡(`CARD_BGM`)은 처음부터 id 키였다. 번호를 키로 쓰는 것은 원래도 위험했다.

**카드마다 비율이 다르다.** 야채 열두 장은 1080×1440 한 판인데(27절에서 통일했다)
과일은 0.699~0.804 로 제각각 왔고, 통일해 달라고 다시 요청하는 대신 **카드별 값을
쓰기로 했다** (`CardTemplate.ratio` 가 원래 카드별 필드다). 대신 **폭에서만 반지름을
재던 자리**가 깨졌다 — 포일이 얼굴을 비껴가게 하는 마스크(`FoilQuiet`)가 "카드가 3:4 라
`1080×rx% == 1440×ry%`" 를 근거로 원을 그리고 있었다. 타원으로 고쳤다. 안 고치면 얼굴
위아래 가장자리에 포일 링이 남고, 젠틀 키위는 얼굴창 자체가 정원이 아니라(322×291px)
더 크게 어긋난다.

**반입 도구를 하나 더 만들었다** (`tools/punch_fruit_holes.py`). 기존 둘 다 새 벌을
부트스트랩 못 한다. `punch_card_slots.py` 는 얼굴을 **크림색 색조**로 찾는 은퇴한
도구라 흰 표시 원에서 채도 문턱에 걸려 100% 실패하고, `card_text_slots.py` 는 대상을
`ART.glob("*-card-slots.webp")` 로 만들어서 **아직 판이 없는 벌을 통째로 건너뛴다.**
새 도구는 구멍만 뚫어 판을 만들고, 글자는 기존 도구가 이어받는다.

그 과정에서 `card_text_slots.py` 의 구멍 둘을 같이 막았다. **드롭폴더에 원화가 없으면
건드리지 않는다** — 예전에는 이미 글자가 지워진 판으로 그냥 진행해서, 과일만 넣고
돌렸을 때 당근·단호박·가지·피망이 카드 폭 전체를 이름칸으로 냈다. 그대로 `--apply`
했으면 멀쩡한 야채 판 다섯 장이 다시 지워질 뻔했다. 그리고 **앵커로 자른 결과가 글자보다
얇아지면 되돌린다** — 복숭아에서 앵커가 522(같은 캔버스인 멜론은 225)로 잡혀 바가
98px 에서 7px 로 줄었고, 그 안에서 글자를 못 찾아 카드가 통째로 건너뛰어졌다.

## 지금 남아 있는 것

[STATUS.md 의 "다음 결정"](STATUS.md#다음-결정) 을 보면 된다. 큰 것만 적으면 산책 게임
화면, 소품 해금 규칙이다.


## 산책 행동을 개별 기록과 메모로 다룬다 (09-05, #144)

사용자 결정으로 버튼을 킁킁·배설·짖기와 자유 메모인 특별한 순간으로 정했다. 옛 행동 데이터는 보존하지 않고 Room v7에서 비운다. 경로는 보존한다. 원본을 5m 묶음으로 합치지 않고 개별 ID로 수정·삭제한다. 개인화 정책은 미정이므로 산책 기록 프로필 계약만 도입한다. 서버 동기화와 확인 경계는 [산책 기록](docs/walk-records.md)에 적었다.

## 일반 사진은 산책 일기, 전봇대 사진은 인증에 귀속한다 (09-05, #151)

사용자 결정으로 도크의 일반 촬영과 선택 전봇대의 인증 촬영을 분리했다. 일반 사진은 셔터 시각의 세션·좌표에 고정하고 Room v9과 앱 전용 영구 파일로 보관한다. 인증 임시 파일이나 점령 시도로 변환하지 않는다. 사진만 남긴 짧은 산책도 보관하지만 운동량 인정 기준은 유지한다. 기존 산책 기록은 지우지 않고 사진 표만 더한다. 서버 사진 계약은 아직 없어 기기 내 보관까지 제공하며, [저장·삭제·검증 경계](docs/walk-diary-photos.md)를 따른다.

## 뽑기가 균등을 버리고 꽝과 레어도를 들였다 (09-07, #199)

사용자 결정으로 한 판에 20% 꽝을 넣고, 곡·무대가 있는 카드를 드물게 했다.
무게는 무대 1 · 곡만 2 · 나머지 4 다.

**균등은 원래 일부러 고른 것이었다.** 카드가 열두 장이고 곡이 아홉 장이던 시절에는,
곡 있는 카드를 레어로 묶으면 턴테이블을 구경도 못 하고 끝나는 사람이 생겼다.
스물다섯 장이 되면서 곡 있는 카드가 열한 장으로 비율이 절반 아래가 되어 사정이
뒤집혔다. 세 번 뽑아 곡을 하나라도 얻을 확률은 74% 에서 49% 로 내려간다.

**무게 목록을 따로 안 적는다.** `weightOf` 가 `CARD_BGM` 과 `IMMERSIVE_SCENES` 에
물어본다 — 곡을 붙이면 그 카드는 그날로 레어가 된다. 두 군데 적으면 곡은 늘렸는데
확률은 그대로인 채로 지나간다.

**꽝은 카드가 없어서 하루 횟수에 안 걸린다.** 횟수는 저장된 카드의 시각을 세서 구하는데
(`drawsLeft`) 꽝은 아무것도 안 남기므로, 그냥 두면 꽝이 공짜가 되어 결국 매일 세 장을
다 받게 된다. 그래서 꽝 시각만 따로 적는 `MissLog` 를 두고 두 목록을 합쳐서 센다.
카드 표에 꽝 줄을 넣지 않은 것은, 얼굴 없는 줄이 이미 "개발 기기에 미리 넣어 둔 카드"
라는 뜻이라 도감에 빈 카드가 뜨기 때문이다.

## 곡이 사람마다 갈린다 (09-07, #199)

망고에 곡이 둘이 되면서 `CARD_BGM` 의 값이 한 곡에서 목록이 됐다. 어느 곡이 걸리는지는
`seedOf(appUserId)` 로 정한다 — **카드 장마다가 아니라 사람마다다.** 카드마다로 하면
망고를 세 장 뽑은 사람의 턴테이블에 망고가 세 줄로 뜬다. 사람 기준이면 그 사람에게
망고 곡은 언제나 하나라 목록 모양이 안 바뀐다.

로그인 전·게스트(`appUserId` 가 `null`)면 첫 곡이다. **로그인하면 곡이 바뀔 수 있다는
뜻이다.** 곡을 카드에 저장해 두면 안 바뀌겠지만 컬럼이 하나 늘고, 이미 뽑아 둔 카드는
그 값이 비어 있다.

## 2026-09-08 · 인증 우선 점령과 홈 시즌 현황 (#205)

전송·방문 인증·점령 정책의 결과를 구분한다. 서버가 보낸 보호 종료 전에는 촬영을 막고,
셔터 직전 허가를 다시 받는다. 같은 claim에 새 challenge/capture를 연결해 같은 산책에서
재도전하며 기존 PHOTO 재전송의 식별자는 보존한다. 홈의 산책 기록 진입 아래에 현재 시즌
조회와 선택 강아지의 점수·점령 수를 연결했다. 서버 규칙이 권위 있고 앱 시계는 표시만 담당한다.

리뷰 후 촬영 진입 사전검사의 보호·시즌 종료 사유도 화면까지 전달하도록 보완했다.
지도 조회 이후 정책이 바뀌어도 위치나 산책 상태 오류로 안내하지 않는다.


## GPS 품질과 행동 기록의 저장 여부를 분리한다 (09-09, #232)

GPS가 나쁘면 행동까지 잃던 흐름을 바꿨다. 원본 content와 표시 pin을 나누며 추정값을
원본 GPS나 점령/사진 인증으로 되돌려 넣지 않는다. 초기 상태를 같은 Room 트랜잭션으로
저장하고, 원래 탭 시각과 최대 8초의 관측 창을 고정해 한 번만 확정한다.

Room 11→12는 기존 데이터와 v1 분석 stamp를 보존한다. 전송할 요청을 영구 동결해
ACK 유실 뒤 수정된 본문에 같은 mutation ID를 재사용하지 않는다. 삭제는 핀·요청 본문까지
제거한다. 서버가 v2 또는 관측 중단 시각을 지원하지 않으면 원본을 기기에 보관한다.
서버 쓰기 활성화와 최소 지원 앱 버전 정책은 별도다. [구현 범위](docs/action-pin-app-integration.md).

## 처음 공개한 산책 보드는 자동으로 교체하지 않는다 (09-10, #276)

새로 종료한 산책은 종료 시각부터의 준비 예산과 로컬 기본 보드를 Room 14에 보관한다.
기한 안에 받은 유효한 서버 보드 또는 마감 때의 로컬 보드를 한 번 공개하고,
그 뒤 AI 응답·재진입·새로고침은 공개 내용을 교체하지 않는다. 사용자 기록의 명시적
추가/삭제/수정과 같은 카드 편집은 계속 반영한다.

13→14는 준비/공개 표만 추가하며 기존 완료 산책에 준비 행을 소급 생성하지 않는다.
이미 공개된 내용에 새 정책을 소급하면 사용자 수정의 기준 장면이 바뀌므로 금지한다.
확정본과 편집본의 다른 기기 전송은 기존처럼 별도 과제다.
[단일 공개의 저장·복구 계약](docs/walk-diary-publication.md).

## 날짜·시각 휠은 톡 눌러야 깨어난다 (09-11, #277)

`DateWheel`·`TimeWheel` 이 공유하는 `Wheel` 은 `LazyColumn` 이라 그 위의 세로 제스처를
남김없이 먹었다. 휠을 쓰는 화면은 전부 `verticalScroll` 폼이어서, 폼을 내리려고 휠 위에
손을 얹으면 폼 대신 날짜가 돌아갔다. **문제의 본체는 폼이 안 움직이는 것이 아니라 그 사이
값이 조용히 바뀌는 것이다** — 한 번 쓸었을 뿐인데 2023-05-14 가 2023-12-14 가 됐다.

세로 드래그 하나로 "폼을 내리려는 손"과 "휠을 돌리려는 손"은 못 가른다. 축이 같아서
속도로도 거리로도 안 갈린다. 어떤 자동 규칙을 넣어도 휠이 늘 이기거나(지금의 버그)
폼이 늘 이겨(휠을 못 씀) 둘 중 하나로 무너진다. 그래서 **가르지 않고 물어보기로 했다.**
잠든 휠은 드래그를 아예 안 받아 그 제스처가 폼으로 가고, 한 번 톡 누른 뒤에만 돌아간다.
깨어난 휠은 폼이 한 칸이라도 움직이면(`onGloballyPositioned` 로 제 자리가 바뀌는 것을 본다)
도로 잠든다.

**되돌리기 번거로운 쪽은 이것이다 — 세 화면이 같이 움직이고 유저가 규칙을 익힌다.**
등록 폼의 생일·급식 시각과 배웅의 떠난 날이 한 번에 바뀌고, #258 의 진료비 방문 날짜도
머지되면 같이 따라온다.

**눌러서 펼친 휠을 깨운 채로 내놓는 것도 재 봤지만 안 했다.** 등록 폼은 탭이 하나 줄지만,
다 고르고 폼을 내릴 때 휠이 깨어 있어 값이 또 조용히 바뀐다. 폴더블 펼침(673dp × 841dp)처럼
휠이 화면을 거의 다 차지하는 기하에서는 휠 바깥을 잡을 자리가 없어 빠져나갈 길이 없다.
탭 하나가 그것보다 싸다. 그래서 **전부 잠든 채로 시작한다.**

휠이 끝에 닿아 남은 스크롤이 폼으로 새는 것은 `KeepScrollInside` 가 막는다 — 원래
`PetFormScreen` 의 private 였고 `BreedGrid` 가 쓰던 것을 `ui/common` 으로 올렸다.
그쪽은 *안이 밖으로 새는 것*, 휠은 *밖으로 갈 것을 안이 먼저 먹는 것* 이라 방향이 반대지만
깨어 있는 동안은 둘 다 필요하다.
