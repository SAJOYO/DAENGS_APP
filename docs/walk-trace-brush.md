# 산책 흔적 브러시 시연

산책 기록 탐색 화면의 첫 단계다. 한 산책을 한 장으로 칠하고 여러 산책이 겹친 곳만 진해지는 표시 방식을 실제 지도에서 확인한다. 일반 사용자 화면에는 아직 진입점을 연결하지 않는다.

## 표시 계약

- 입력은 산책 ID와 `hex-v1`의 양수 지지 영역 셀 집합이다. 동일 산책의 중복 셀은 한 번만 칠한다.
- `occupancy`·`peak`·행동 개수·재통과 횟수는 불투명도에 사용하지 않는다. 저장된 공간 데이터나 기존 `walk_utilization` 평균 지도 계약을 변경하지 않는다.
- 한 장의 기본 알파는 고정값 0.28이다. 완전히 덮인 부분의 두 장 알파는 `1 - (1 - 0.28)^2 = 0.4816`이다. 전체 산책 수나 화면 최대값으로 재정규화하지 않는다.
- 가장자리는 부드럽게 처리한다. 표시 마스크는 저장 셀의 연속적인 표현이며, 실제 GPS 중심선이나 시설 진입의 증거가 아니다.
- 지도에서 숨긴 산책은 합성에서만 제외한다. 복원할 때 같은 마스크를 사용한다.
- 서로 먼 영역은 세계 좌표에 고정된 별도 타일로 만든다. 빈 공간을 포함한 거대한 단일 비트맵을 만들지 않는다.

## 계산과 지도 연결

`TraceBrush`는 Android에 의존하지 않는 표시 전용 계산이다. Web Mercator의 고정 래스터 위에 셀의 합집합을 만들고 안쪽 축소·Gaussian·smoothstep을 적용한다. 원래 육각형 외곽으로 다시 자르지 않는다. 서로 떨어진 성분은 독립적으로 처리하며, 작은 성분이 전부 사라질 때는 약한 축소 조건으로 다시 만든다.

기본값은 픽셀 간격 2u, 타일 128×128, 안쪽 축소 2u, Gaussian sigma 5u, smoothstep 0.12~0.5다. `u`는 위도에 따라 실제 거리와 달라지는 Web Mercator 단위다. GEO `c8d0cb1`의 `2026-09-07-walk-overlap-thin-brush.md` 명세를 참고했으며, 밀도에 따라 알파를 바꾸는 실험 정책은 가져오지 않았다.

첫 실제 지도 시연에서 상한 0.9·기본 알파 0.14는 한 셀 폭의 흔적을 지나치게 옅게 표시했다. Gaussian과 하한 0.12는 유지하고 상한만 0.5로 낮춰 좁은 흔적의 중심을 채웠다. 따라서 색이 생기는 바깥 영역이나 떨어진 영역의 연결 여부는 이 조정으로 늘어나지 않는다. 가장자리가 차지하는 비율이 큰 매우 작은 흔적은 넓은 면 중앙보다 옅게 보일 수 있다.

한 산책은 최대 5,000셀·256타일, 성분별 계산은 합계 512타일까지 제한한다. 합성은 최대 400산책·입력 1,024타일·출력 256타일이다. 제한을 넘으면 일부를 조용히 누락하지 않고 실패를 반환한다. 대량 산책의 캐시·뷰포트별 부하 조절은 실데이터 연결 단계에서 별도로 측정한다.

출력은 북쪽부터 시작하는 행을 가진 `TraceRasterTile`이다. `NaverWalkTraceLayer`는 합성된 알파를 비트맵에 기록하고 실제 Naver `GroundOverlay`에 올린다. 지도 이동·확대는 원본 마스크나 기본 알파를 변경하지 않는다. 계산과 비트맵 준비는 화면 스레드 밖에서 수행하며, 레이어 교체·화면 이탈 시 기존 오버레이를 분리한다.

표시 마스크를 행동 확률·방문 횟수 등 통계 계산에 되먹이지 않는다. 산책 선택과 행동 조회는 다음 단계의 별도 경계다.

## 로컬 실행

기존 앱 개발 환경을 사용한다. 지도 Client ID는 저장소에 넣지 않고 `local.properties`의 `daengs.naverMapClientId` 또는 `DAENGS_NAVER_NCP_KEY_ID` 환경 변수로 제공한다.

```powershell
.\gradlew.bat :app:assembleDebug
adb install -r app/build/outputs/apk/debug/app-debug.apk
adb shell am start -n com.daengs.app/.ui.walk.WalkTraceBrushLabActivity
```

샘플임을 명시한 debug 화면이며 로그인·서버·사용자 DB에 의존하지 않는다. 지도 SDK 인증이 실패해서 배경이 비어 있으면 실제 지도 시연의 완료로 보지 않는다.

## 검증 범위

단위 테스트는 산책 중복 거부, 같은 산책의 합집합, 서로 다른 산책의 고정 알파 합성, 숨김·복원, 떨어진 영역과 작은 흔적, 타일 경계의 연속성, 입력 제한을 확인한다. 실제 지도에서는 한 장/두 장 전환, 좁은 흔적과 떨어진 흔적, 숨김·복원, 줌·이동의 정렬을 확인한다.

이 단계는 브러시와 지도 표시의 기술 시연이다. 산책별/모아보기 화면, 행동 검색, 실제 서버·Room의 데이터 연결은 후속 작업이다.

### 2026-09-10 확인 결과

- `TraceBrushTest` 9개, `MapScenePolicyTest` 4개, `SpatialDiaryGeometryTest` 4개 통과. 실제 지도 확인 후 표시 정책을 조정하고 영향을 받은 `TraceBrushTest` 9개를 다시 실행해 통과했다.
- `:app:assembleDebug -PslimAbi=x86_64 -PversionName=brush-preview-251` 성공. Pixel 8 에뮬레이터(Android 37.2)에 설치한 결과 `Success`와 실행 버전을 확인했다.
- 실제 Naver 지도 배경 위에서 한 산책/두 산책, 숨김·복원, 확대·이동과 떨어진 한 셀 흔적을 확인했다. 숨겨도 선택 수 2회는 유지되고 표시 수만 1개가 된다. 관찰한 범위에서 타일 경계의 갈라짐은 없었다.
- 독립 코드 리뷰에서 이번 시연 범위의 확정 오류는 발견하지 못했다. 전체 테스트·원격 CI·실데이터 대량 부하 검증은 실행하지 않았다.
