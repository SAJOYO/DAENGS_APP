# STATUS — 댕스 안드로이드 앱 (홈 화면 · 미니룸)

> 2026-09-03 기준. 처음 오신 분은 [README](README.md) 먼저 보세요.

## 지금 상태

- **서버 점유 조회 공급자:** 일반 debug는 로컬 플레이를 유지하고, 명시적인 테스트 빌드에서 같은 지도에 서버의 강아지·인증 상태를 읽는다. 로딩/실패와 미점유를 구분하며 서버 모드의 게임 액션은 후속이다. [PR #161 설정과 검증 범위](docs/territory-server-browsing.md).

- **실제 산책 스토리보드:** GPS finalize·기록 동기화 뒤 장면 분석을 요청하고 검토 화면에 연결했다. 원본 변경·지연 응답을 버전으로 구분하며 Room 9→10에서 기존 사진·검토본을 보존한다. 서버 SQL 적용과 기능 배포가 선행되어야 한다. [연결과 재시도](docs/walk-storyboard-live.md).

- **전봇대 접근·성공 피드백:** 선택 장소의 짧은 마커 확대/링 강조와 미인증·인증 발자국 효과를 추가했다. 같은 산책/장소의 준비 효과 중복을 막고 시작 전·일시정지·사진 대기를 구분한다. [PR #155의 효과와 검증 경계](docs/territory-feedback.md). 실기기 Naver SDK 움직임은 확인이 남았다.
- **geo 스토리보드 실험:** debug 지난 산책에서 합성 GPS + 실제 환경 캐시의 장면 JSON을 불러와 기존 검토 UI로 편집한다. 같은 세션의 원본 갱신 시 문구·숨김·마지막 검토본을 보존하며 재확인을 표시한다. 운영 산책·AI는 미연결이다. [실험 방법](docs/geo-storyboard-lab.md).

- **산책 일기 사진:** 도크의 일반 촬영을 전봇대 인증과 분리하고, 셔터 위치의 사진 Pin·로컬 영구 저장·지난 기록 다시 보기·삭제를 연결했다. Room v9는 기존 경로/기록/스토리보드를 보존한다. 사진 업로드와 실제 기기 검증은 남았다. [PR #151의 범위와 검증](docs/walk-diary-photos.md).
- **산책 지도 UI 이식:** 시작 전 점령지 둘러보기와 진행 중 장소 액션을 분리하고 장소별 점령 강아지 선택, 작은 도크·GPS 점·지도 도구를 연결했다. [PR #148의 범위와 검증](docs/walk-territory-browsing.md).
- **스토리보드 검토 화면:** 산책 상세에서 시간순 장면의 문구 편집·숨김·복원·근거 확인과
  검토 완료 사본을 로컬에 저장한다. 환경 분석 연결은 위 실제 산책 항목을 참고한다.
  AI 일기와 검토본 서버 동기화는 후속이다. [사용 방법과 범위](docs/walk-storyboard-review.md).

- **산책 행동/메모 후속 구현:** 킁킁·배설·짖기와 특별한 순간 메모, 기록 상세·정정·삭제, Room v7, 서버 기록 동기화와 산책 기록 프로필 클라이언트를 추가했다. [현재 범위와 확인 경계](docs/walk-records.md)를 먼저 읽는다. 아래 PR #95까지의 설명은 이전 입력 구조이며, 당시 로컬 전용이던 기록은 별도 entries 계약으로 확장했다.
- **점령 게임 3단계 촬영·판정 연습** — [PR #142](https://github.com/SAJOYO/DAENGS_APP/pull/142).
  debug 지도에 실제 카메라 촬영과 프로세스 범위의 페이크 판정 대기열을 연결했다.
  인증 강화·탈취·재촬영·기존 사진 재시도를 제공한다. 온라인/VLM은 후속이며 실제 카메라의
  기기 검증은 남았다. [체험과 검증 범위](docs/territory-photo-preview.md).

- **점령 게임 2단계 지도 연결** — [PR #140](https://github.com/SAJOYO/DAENGS_APP/pull/140).
  debug 빌드에서 실제 주변 점령지에 미인증/인증 표시와 접근 범위를 겹치고, Pin 영역표시로
  빈 장소를 미인증 점유하는 로컬 연습 흐름이다. 점유는 메모리 전용이며 온라인
  공유는 후속 단계다. 체험 방법과 검증 범위는 [연결 문서](docs/territory-map-actions.md).

- **안드로이드 네이티브** (Kotlin + Jetpack Compose). 유니티나 게임 엔진은 안 쓴다.
- 홈 화면은 상단바 → **미니룸 카드** → AI 챗봇 입력창 → 산책 요약 → 하단 내비 구조다.
  홈의 산책 요약은 **오늘 걸은 것을 진짜로 합산해서** 띄우고, TODAY 카드와 오늘의
  한 마디는 **실제 날씨로 짓는다** (창밖 그림과 같은 값이라 서로 안 어긋난다).
  챗봇은 **사진 진단이 실제로 돌고, 자유 텍스트도 이제 오케스트레이션
  서버(`POST /assistant/query`)로 붙는다** — 산책·훈련 등 자연어 질문에 실제 답이
  온다. 보행 분석을 부르는 로컬 키워드 라우팅은 지웠고, 서버가 HANDOFF 로 보내면
  그때 기존 사진/보행 흐름을 연다 (아래 참고).
- **챗봇 자유 텍스트가 오케스트레이션에 붙어 있고, 위치가 필요한 질문도 한 번에
  답한다.** `POST /assistant/query` 에 지금 좌표를 같이 실어서, "오늘 산책 나가도
  괜찮을까?" 가 되묻는 문장에서 멈추지 않는다. 좌표를 못 구하거나 한국 밖이면
  **좌표만 빼고 질문은 보낸다** — 서버 스키마가 `extra="forbid"` 라 빈 칸을 넣으면
  질문이 통째로 422 로 죽는다. 답변 말풍선은 **길게 누르면 복사**된다.
- **사진·영상을 앱 안에서 찍는다.** 피부 사진은 가운데 네모, 보행 영상은 강아지
  실루엣이 **찍는 동안** 프리뷰 위에 떠 있다. 시스템 카메라로 던지면 그 위에
  아무것도 못 얹어서 찍고 나서야 알았다. 가이드에 맞춰 찍으면 확인 화면이 그 네모에서
  시작해 **확인만 하면 된다.** 카메라 권한을 거부하면 찍지는 못하고 그렇게 말해 준다 —
  갤러리에서 고르는 길은 그대로다.
- 미니룸은 **12 × 12 아이소메트릭 격자**다. 방 그림에 자를 대서 맞춘 원근 사각형이라
  소품이 마룻바닥 무늬에 정확히 앉는다.
- **방·소품·강아지가 전부 픽셀 아트 PNG**다. 팀 다른 저장소
  (`frankie516c/dog-training-rag`)에서 만든 팩을 반입해 쓴다.
  에셋 248장(`drawable-nodpi` 9.7MB · `assets` 23MB — 과일 카드 26장이 11MB 다).
  **APK 는 디버그 137.8MB · 릴리스 129.7MB 다.** 그림이 아니라 네이버 지도의
  `libnavermap.so` 가 CPU 4종만큼 들어가서 `lib/` 만 91MB 다. `-PslimAbi=arm64-v8a`
  로 빌드하면 63MB 가 된다 — AAB 로 올리면 기기마다 한 종만 내려간다.
- **테마 6종** (세이지·벚꽃·민트·라벤더·하늘·버터). 방·소품 아홉 장이 통째로
  리컬러된 팩이라 나뭇결과 픽셀 셰이딩이 살아 있다. 색을 덧칠하는 방식이 아니다.
- **견종 27종.** 시바·포메라니안·푸들은 색 변형이 셋씩 있고, 닥스훈트가 단모 갈색·
  단모 검정·장모 베이지로 들어와 있다. 견종마다 보이는 크기·바닥에서 차지하는 반경·
  걷는 속도가 다르다 (치와와 9.5% ~ 보더콜리 18.5%). 강아지 4마리가 방 안을 스스로
  돌아다니고, 가구는 못 지나가지만 러그·방석 위로는 지나간다.
  **방에는 등록한 강아지가 선다** — 27종 전부 걷기 시트가 있다. 스피츠·래브라도·
  골든 세 종은 아직 목록을 못 받아 온 자리와, 그림이 없는 견종(믹스)의 대역으로 쓴다.
- **견종마다 얼굴 그림이 따로 있다.** 상단바와 챗봇의 프로필 아바타가 이걸 쓴다.
  방 안 강아지는 도트인데 얼굴은 사실풍이라 화풍이 다른데, 저쪽이 처음부터 그렇게
  나눠 그렸다. 32dp 까지 줄어드는 자리라 도트로는 눈코가 뭉개진다.
  **상단바 얼굴은 대표 강아지의 견종을 따른다.** 그림이 없는 견종(믹스 등)이면
  발자국 얼굴로 떨어진다 — 아무 얼굴이나 고르면 사용자가 자기 개가 아닌 얼굴을 본다.
- **방은 상자 안에 통째로 들어간다.** 바닥 앞모서리·흰 굽도리·울타리가 잘리지 않고,
  세로 정렬은 투명 여백을 걷어낸 **칠해진 영역** 기준이라 위(12.6dp)가 아래(18.7dp)보다
  좁다. 에뮬 Pixel 7 기준 방 그림 270 x 329dp, 좌우 여백 70.9dp.
- **소품 배치**는 드래그로 옮기고, 인벤토리에 넣었다 뺄 수 있다. 기기에 저장돼서
  앱을 완전히 껐다 켜도 그대로다.
- **턴테이블을 누르면 내 카드의 음악이 나온다.** 뒷벽 창문 아래 붙박이라 못 옮기고
  인벤토리에도 안 뜬다. 누르면 방 위로 판이 올라와 곡이 있는 카드를 늘어놓고,
  트거나 내려받을 수 있다. **목록은 내가 뽑은 카드에서 나온다** — 열두 종 중 다섯
  종에만 곡이 있어서, 한 곡도 없으면 "노래를 가진 카드를 뽑아보세요" 와 뽑으러 가기가
  대신 뜬다. 빈 판을 보여 주면 붙박이가 고장 난 것으로 읽힌다.
  내려받기는 저장소 권한을 안 쓴다(사용자가 자리를 직접 고르는 방식).
- **문을 누르면 열린다.** 문 너머로 바깥 풍경이 보이고 바닥에 볕이 번진다.
  산책 게임 진입점으로 쓸 자리다.
- **산책기록 탭에서 실제 GPS 동선을 기록한다.** 시작·일시정지·재개·종료가 되고,
  활동 시간·거리·기록 점 수와 경로선이 지도에 즉시 보인다. 화면을 나가거나 꺼도 위치
  Foreground Service가 이어 받으며 알림에서 일시정지·종료할 수 있다. 원본 위치는 화면용
  흔들림 필터보다 먼저 Room에 저장하고, 끝나면 서버로 올라간다(PR #47). 원본 chunk가
  모두 올라간 `raw_uploaded`와 서버 계산까지 끝난 `derived`를 구분해서, 중간에 앱이
  꺼지면 finalize만 다시 시도한다(PR #71). 끝난 산책은 세션별 unique WorkManager
  작업으로 넘겨 네트워크가 돌아오면 앱 화면을 다시 열지 않아도 이어 보낸다(PR #91).
  저장된 산책은 완료 직후와 지난 기록에서 같은 원본 경로를 읽으며, 출발·도착 핀과
  끊긴 구간을 구분해 그린다. 완료 직후에는 경로 가까이를 눌러 그 시점의 활동 시간·
  누적 거리·구간 속도·GPS 정확도를 볼 수 있고, 세로·가로 전환 뒤에도 선택이 남는다
  (PR #79). 산책 중 남긴 탐색·교류·특별 행동은 누른 시각의 GPS 좌표와 함께 Room에
  원본으로 남아, 완료 직후뿐 아니라 앱을 다시 연 뒤 지난 산책 상세에서도 같은 장소로
  복원된다(PR #95). 장소 묶음은 저장하지 않고 현재 5m 규칙으로 다시 계산하며, 행동은
  아직 서버 계약이 없어 이 기기에서만 보낸다. DB는 이 원본을 위해 v6으로 올렸다.
  AVD가 플랫폼 mock 표식을 빠뜨려도 실행 환경을 함께 확인하므로, 에뮬레이터 경로가
  실제 기기 산책 증거로 저장·업로드되지 않는다.
  **데리고 나갈
  아이를 여러 마리 고를 수 있고**, 지난 산책 목록에 그 아이들 이름이 뜨고 아이별로
  거를 수 있다(PR #49). 여러 산책의 봉인된 공간 셀을 강아지·날짜·비/눈·낮/밤으로
  조회하는 서버 계약과 `hex-v1` q/r 셀을 지도 위 육각형으로 되돌리는 순수 계산도
  앱에 들어왔다. 아직 화면에는 그리지 않는다 — 다음 작업은 이 결과를 별도 Walk
  레이어로 `MapScene`과 네이버 `PolygonOverlay`에 연결하는 것이다.
- **창밖·문밖이 실제 시각·날씨를 따른다.** 시간(낮·밤) x 날씨(해·비·눈) 여섯 벌이고,
  눈일 때는 잎이 다 떨어져 있다. 위치 권한을 받아 Open-Meteo 를 한 번 부른다 —
  **API 키가 없어서 받자마자 돌아간다.** 권한 거부·네트워크 실패면 기기 시각으로
  낮·밤만 잡고 맑음으로 떨어진다. 개발자 도구에서 여섯 벌을 넘겨볼 수 있고,
  **고르면 실제 날씨를 이긴다.**
  풍경은 테마를 안 탄다 — 테마마다 다른 것은 창틀·창살뿐이라 한 벌이 6종을 다 덮는다.
- **네오 도감.** 오른쪽 빈 벽에 걸린 액자를 누르면 홀로그램 트레이딩 카드 25장이
  **야채 12 · 과일 13** 탭으로 갈려
  열린다. **액자에는 기본으로 발자국이 걸려 있고**, 뽑은 카드 중 하나를 골라 걸 수
  있다 — 예전에는 저쪽 배추 카드가 박혀 있어서 한 장도 안 뽑은 사람의 방에도 남의 개가
  걸려 있었다 (PR #86). 카드를 확대하고 **문지르거나 폰을 기울이면 포일이 돈다.** 카드를 한 번 더
  누르면 **설명 시트**가 뜬다 — 부제·코드·기술·플레이버·에디션이고, 문구는 정본인
  `SAJOYO/DAENGS_CARDS` 의 `cards.mjs` 에서 그대로 옮겼다. **No.01 배추 ·
  No.10 고구마 · No.12 상추**는 꾹 누르면 카드 "안으로" 들어가 시차 평면 일곱 장짜리
  무대가 된다 — 이슬 맺힌 텃밭 · 보랏빛 결계 · 황금 무대다. **무대에 선 주인공도
  우리 아이 얼굴이다** — 야채 몸통은 그대로 두고 얼굴 자리만 카드와 똑같이 뚫어
  갈아 끼운다(`tools/punch_subject_face.py`). 전신 그림을 새로 받을 필요가 없었다.
  **포일과 이머시브는 서로 독립이라** 상추는 `metal` 포일을 유지한 채 이머시브를 갖는다.
  **No.11 토마토는 이머시브가 아니라 팝아웃**이다 — 확대 뷰에서 카드를 짚고 있으면
  주인공이 카드 테두리 밖으로 떠오른다. 저쪽은 마우스를 올려놓으면 나는데 터치에는
  hover 가 없어서, 짚고 있는 동안으로 옮겼다. 어느 카드가 이머시브인지는
  `IMMERSIVE_SCENES` 표 하나가 정한다. 카드마다 레이어 원화 넉 장·곡·실측값 둘이
  필요해서, 저쪽에서 그 한 벌이 와야 늘어난다.
  **전부 Compose 네이티브다** — 한동안 웹 데모를 WebView 로 실었지만 앱으로 통일했다.
  카드 그림만 팀 저장소 `SAJOYO/DAENGS_dev` · `SAJOYO/DAENGS_CARDS` 에서 가져다 쓴다
- **우리 아이 얼굴로 카드를 뽑는다.** 사진을 고르면 얼굴만 따서(ML Kit 온디바이스)
  자리를 비운 야채 원화의 구멍에 끼우고, 이름과 생일에서 만든 번호를 찍는다.
  **12종 균등, 하루 3번**이고 자정에 찬다. 안 뽑은 칸은 검은 잠금판이라 무엇인지
  안 보이고, 뽑으면 그 자리가 채워진다. 같은 야채를 여러 번 뽑으면 한 칸에 모이고
  ×N 이 붙는다 — 사진마다 표정이 달라서 위아래로 넘겨 다 볼 수 있다.
  **사진이 서버로 안 간다** (누끼가 기기에서 돈다). 카드는 기기에만 남고
  (`daengs_card.db` + `filesDir/cards/`) **강아지를 지워도 카드는 남는다** —
  카드는 계정에 매이고 강아지는 표시다 (PR #19 · #72).
- **카드를 이미지로 저장한다.** 설명 시트에서 폭 1080 PNG 로 나간다 (높이는 카드 비율을
  따른다 — 야채는 1440 이고 과일은 카드마다 다르다). 화면이 그리는
  함수를 그대로 화면 밖에서 불러서, 파일이 화면과 다를 자리가 없다.
  **앨범 권한을 안 쓴다** — 사용자가 저장할 곳을 직접 고르는 방식이다 (PR #83).
- **강아지 배웅하기.** 삭제와 다른 길이다. 아이가 보내는 편지가 뜨고, 간 날은 손으로
  적는 게 아니라 다이얼로 고른다. 배웅한 아이는 목록에서 사라지지 않고 자기 자리를
  갖는다 — 프로필 · 편지 보기 · 추억 보기(준비 중)다. 한 마리만 키우다 배웅해도
  그 아이가 대표로 남는다. **간 날은 서버가 든다** (`pets.farewell_on`).
  삭제 창에도 배웅하기가 있다는 것을 알려 준다 (PR #76~#78 · #80).
- **배웅한 아이는 방에서 그대로 뛰어논다.** 멈춰 세우지 않는다 — 배웅은 방에서
  내보내는 것도 세워 두는 것도 아니다. **쉬고 있을 때만** 곁에 작은 분홍 하트가 뜬다.
  처음엔 흰 무지개였는데 실기기에서 **와이파이 표시로 읽혀서** 바꿨다 (PR #84 · #85).
- **홈의 TODAY 날씨 카드를 접었다 편다.** 접으면 날짜 한 줄만 남고, **가로폭은
  안 변한다** — 폭까지 줄면 방 위에서 카드가 좌우로 널뛴다 (PR #73~#75).
- **내 주변.** 하단 탭에서 병원·약국·카페·펫샵을 지도에서 찾는다. 카테고리 칩 하나씩
  검색하고, 카드에서 길찾기(네이버 지도로 넘김)와 전화를 건다. **예전 이름은
  "산책기록" 이었는데 여는 화면이 처음부터 지도라 이름을 화면에 맞췄다** — 산책
  기록은 홈의 산책 요약 카드에서 펼치기로 자리가 정해졌다.
  **지도 타일은 `daengs.naverMapClientId` 가 있어야 나온다** — 없으면 격자만 뜨고
  마커·검색·카드는 정상이라 앱이 고장 난 것처럼 보인다 (README 참고).
  지도도 앱 팔레트를 따른다 — 타일이 오기 전 바탕이 크림이고, 건물은 2D 로 눕히고,
  지도를 살짝 밝혀 뒤로 물린다(오버레이에는 안 먹어서 마커는 선명하다).
  **마커 8색은 채도만 낮췄다** — 색상이 곧 카테고리라 통일하면 병원과 카페를 구분할 수
  없다. 지도 타일 자체의 색은 콘솔 Style Editor 에서 만든 스타일(`daengs.naverMapStyleId`)이
  정하고, **없으면 기본 네이버 지도**다.
- **개발자 도구** — 화면 오른쪽 위 `DEV` 토글. 격자선·소품 발자국·강아지 반경을
  방 위에 덧그리고, 견종·창밖·프로필 얼굴을 바꿔가며 볼 수 있다. 저장하지 않는
  세션 한정 스위치다. **디버그 빌드에만 들어간다** — 릴리스에는 같은 시그니처의
  빈 함수가 들어가서 코드 자체가 스토어 APK 에 없다 (`app/src/release/`).
- **카카오 로그인.** 랜딩 화면 → 카카오 → 우리 서버 세션. 토큰은 기기에 암호화해
  저장하고, 만료되면 조용히 재발급한다. 설정이 없으면 랜딩이 그 사실을 알려 주고
  "둘러보기"로 방까지 들어가진다 — **그 버튼은 디버그 빌드에만 있다.**
  출시 앱은 로그인이 필수다.
- **마이.** **상단바의 프로필 얼굴을 눌러 연다** — 하단 바 다섯째 자리는 저장소로
  갔다. 열려 있는 동안에도 하단 바가 먹는다 (PR #66 · #67).
  프로필 얼굴·로그아웃·회원 탈퇴·버전. 탈퇴는 서버 계정과 이 기기의
  방 배치를 같이 지운다(방은 서버에 사본이 없다). **실패하면 아무것도 안 지운다** —
  서버에 계정이 살아 있는데 앱만 잊으면 사용자는 지워진 줄 알고 다시 시도할 길도
  잃는다. **내 강아지 카드**가 여기 있고, 카드를 눌러 대표를 바꾸거나 정보를 고친다.
- **등록한 강아지가 방에 산다.** 두 마리를 등록하면 방에 두 마리가 돌고, 견종을
  고치면 그 아이만 바뀐다. 그림이 없는 견종(믹스)은 방에서만 대역이 서는데,
  **강아지 id 로 정해서 앱을 껐다 켜도 같은 종**이다.
- **강아지 등록.** 로그인했는데 강아지가 없으면 온보딩으로 간다. 이름·견종·성별·
  중성화·몸무게·생일(모르면 "가족이 된 날")을 받고, 이름 말고는 **전부 비워 둘 수
  있다** — 필수로 하면 모르는 사람이 아무 값이나 넣어서 그 값이 데이터로 못 쓰인다.
  **강아지는 계정에 딸린다.** 기기에 저장하지 않고 서버(`/app/pets/*`)가 진짜다.
  첫 아이가 자동으로 대표가 되고, 대표를 지우면 다음 아이로 승계한다 — 그건
  **서버가 정하는 규칙**이라 앱은 무엇을 바꾸든 목록을 다시 받아 온다.
  한 계정 **5마리**까지고, 이 숫자는 **재서 정했다** — 기본 배치의 방에서 마리 수를
  늘려가며 그림이 겹쳐 보이는 쌍을 셌더니 5마리는 프레임당 0.50 쌍인데 8마리에서
  1.15 로 1을 넘는다(= 언제 봐도 어딘가 겹쳐 있다). `DogCrowdingTest` 에 표가 있다.
- **사진 피부 진단.** 채팅 입력줄의 카메라 버튼 → 사진찍기/첨부하기 → **가이드
  프레임으로 병변 자리를 지정** → 서버 판정. 결과는 말풍선에 확률·분포로 그린다.
  **병명을 하나 골라 크게 쓰지 않는다** — 저쪽 계약에 "1등" 필드가 없는 것이 그
  설계이고, 화면이 그걸 대신하면 안 된다. 서버는 `DAENGS_dev` 의 `skin-screening`
  컨테이너이고 `daengback.weareithero.cloud/screen` 으로 붙는다.
  음성 버튼은 자리만 잡아둔 껍데기다.
- **이머시브 배경음.** 이머시브 무대마다 곡이 하나씩 깔린다. 진입 연출에 맞춰 볼륨이
  차오르고, 화면을 나가거나 홈 버튼을 누르면 멈추면서 오디오 포커스를 놓는다.
  아래 글자 옆 스피커로 끌 수 있고 선택은 기억한다.
- 단위 테스트 **498개** (클래스 75개, 전부 통과). 좌표 변환·배치·앞뒤 정렬·문 터치·
  견종 규격·창밖 매핑에 더해 진단 서버 응답 파싱, 산책 기록·저장·표시 계약,
  행동 원본의 장소 재구성·DB 마이그레이션·삭제 계약, 강아지 계약 파싱,
  방이 몇 마리까지 담기나, 그리고 카드 쪽으로 뽑기 확률 분포·
  하루 3번 경계·도감 칸 집계·내보낸 파일 이름·턴테이블 소유 곡을 고정한다.

## 사용한 기술

- Jetpack Compose `Canvas` 직접 그리기. 화면 하나에 방·소품·강아지를 전부 그린다
- **원근 사각형(bilinear) 좌표계.** 순수 아이소메트릭 마름모가 아니라 방 그림에
  픽셀로 맞춘 사각형이고, 화면→격자 역변환은 뉴턴 반복 8회로 푼다
- `ArtBox`(크기 · 기준점 · 차지하는 칸)라는 계약 하나로 **배치 · 터치 판정 · 앞뒤
  정렬 · 점유 계산**이 전부 돌아간다. 아트를 도형에서 PNG로 갈아엎을 때 이 덕을 봤다
- 픽셀 아트라 `FilterQuality.None` 으로 그린다. 기본값이면 확대할 때 뿌옇게 번진다
- 에셋은 `drawable-nodpi` + WebP(q92). 방 팩은 PNG 그대로면 57MB 인데 거치면 7.5MB 다.
  지금 폴더 전체가 9.7MB (방·소품 60 · 강아지 54 · 창밖 6 · 문밖 6)
- 파이썬 도구(`tools/`)로 에셋을 가공한다. 배경 뚫기 · 시트 조각 털기 · 문 윤곽 뜨기.
  전부 PEP 723 이라 `uv run` 으로 바로 돈다

## 다음 결정

1. **산책 기록을 어떻게 게임으로 만들 것인가.** 순수 GPS 기록·로컬 저장·지도 경로는
   끝났다. 문을 산책 탭 진입점으로 연결할지, 전봇대 마킹·영토·아이템 획득을 실제로
   넣을지는 아직 제품 결정이다
2. **소품 해금 규칙.** 저쪽 목업은 과제를 깨면 소품이 풀리는 구조다. 우리도 그렇게
   갈지, 어떤 행동이 무엇을 푸는지 데이터 계약을 정해야 한다
3. **포일 세기.** 지금 `shineOpacity` 0.30 이다. 웹판과 나란히 보고 정하면 된다
4. **텃밭 입구.** 채소 카드라 텃밭이 주제에 더 맞는데 화면이 아직 없다. 생기면 액자와
   같은 곳으로 들어가는 입구를 하나 더 붙이면 된다
5. **방을 시안만큼 크게 하려면 무엇을 바꿀 것인가.** 지금 좌우 여백이 70.9dp(화면의
   17%씩)다. 방 보이는 폭은 `0.7496 x 상자 높이` 라 시안처럼 좌우 20dp 만 남기려면
   상자가 495dp 여야 하는데 지금 360dp 다. **카드를 다 없애도 모자란다.**
   카드를 조여 방을 키우는 안(챗봇 146->120 등)은 만들어 재보고 접었다 —
   이득이 방 폭 7% 인데 터치 영역을 내주고, 그러고도 시안에 못 간다
   ([HISTORY 16절](HISTORY.md)). 남은 길은 둘이다.
   **가로형 방 에셋을 새로 그리거나**(지금 비율 0.80, 필요한 건 1.1 근처),
   **홈 화면을 스크롤 구조로 바꾸거나**. 둘 다 기획 결정이라 별도 과제로 둔다
6. **보더콜리 눈** — 저쪽에 게임 크기에 맞춰 다시 그려달라고 요청해둔 상태.
   새 시트가 오면 반입한다 (아래 "알려진 문제" 참고)

7. **출시 준비.** 예전에 여기 적혀 있던 두 가지(서버 TLS · 릴리스 서명 키)는
   **둘 다 해결됐다.** 지금 남은 것은 이렇다.

   - **앱 안 개인정보처리방침 링크와 AI 답변 신고** — PR #68 (`fix/play-release-blockers-1-3`).
     **frankie516c 담당이고 우리는 안 건드린다.** 다만 그 브랜치는 `dev` 보다 73커밋
     뒤처져 있고, `MyRow` 가 `dev` 에서 `SettingRow` 로 바뀌어서 **지금 그대로
     머지하면 텍스트 충돌 없이 컴파일이 깨진다.** PR #65 는 그 부분집합이라 같이
     머지하면 방침 줄이 두 번 뜬다
   - **폰에 뜨는 이름을 `댕스` 로** — PR #87 (`values-ko/strings.xml`)
   - **보행 서버 인증** — PR #64. 아래 "알려진 문제" 의 그 항목이다
   - **Play Console 쪽 (코드 아님)** — 데이터 안전 양식 · 앱 아이콘 · 그래픽 이미지 ·
     소개글. 권한은 인터넷·위치·포그라운드 서비스·알림·카메라뿐이고 **저장소·앨범
     권한이 없다** (사진은 시스템 선택기, 저장은 SAF). 양식을 채울 때 이게 근거다
   - **AAB 업로드 → 비공개 테스트 12명 x 14일**
   - **용량을 미리 보고 갈 것** — 릴리스 APK 가 129.7MB 다 (아래 "알려진 문제")

## 알려진 문제

- **보더콜리 눈이 무섭게 보인다.** 얼굴 털이 검은데 눈 속이 더 어두워서 눈이 파인
  구멍처럼 읽히고, 그 안의 흰 반짝임만 눈알처럼 떠 보인다. 화면에서 얼굴이 126px 라
  홍채 색은 사라지고 명도만 남기 때문이다. 우리 쪽 후처리로 고치는 안을 몇 가지
  만들어봤지만, **원본을 게임 크기에 맞춰 다시 그리는 게 맞다고 판단**해 저쪽에
  요청했다
- **APK 가 130MB 다.** 그림이 아니라 네이버 지도의 `libnavermap.so` 가 CPU 4종만큼
  들어가서 `lib/` 만 91MB 다 (arm64 23.7 · x86_64 25.0 · x86 24.3 · armeabi-v7a 18.0).
  `-PslimAbi=arm64-v8a` 로 빌드하면 63MB 가 되지만 **인텔 PC 에뮬레이터에서 안 돌아서**
  기본은 꺼 뒀다. AAB 로 올리면 기기마다 한 종만 내려가므로 실제 설치 크기는 훨씬
  작다 — 다만 **스토어에 올리기 전에 이 숫자를 알고 가야 놀라지 않는다**
- **진단 서버에 인증이 없다.** 지금 `/screen/` 은 아무나 사진을 올릴 수 있다.
  개발·시연에는 괜찮지만 출시 전에는 막아야 한다. **PR #64 가 이걸 다루고 있다**
- **카드 열 장의 번호판에 모자이크가 남아 있다.** 저쪽 카드에 인쇄돼 있던 글자를 지운
  자리인데, 배추·고구마·상추만 제대로 지워졌다. 나머지는 우리가 메워 봤지만 (가로로
  늘리면 번짐, 세로로 늘리면 줄무늬, 아래에서 복제하면 이음매) 다 실패했다.
  **제대로 뚫린 원화가 와야 풀린다** — 이름 자리는 해결됐다
- **구멍이 작은 카드는 액자에서 얼굴이 안 보인다.** 액자 속이 화면에서 60px 남짓인데
  고구마는 얼굴 구멍이 카드의 12.1% 라 얼굴이 15px 이 된다. 배추(19.8%)는 잘 보인다.
  **카드마다 구멍이 3.4배 차이 나는 데서 오는 것**이라 액자 쪽에서 고칠 수 없다
  ([docs/card-holes.md](docs/card-holes.md))
- **`MyScreen.kt` 가 `SettingRow` 를 두 벌 갖고 있다.** `ui/common` 에서 import 하면서
  (`MyScreen.kt:48`) 같은 이름의 `private fun` 도 남아 있다 (`:580`). 파일 안의 것이
  이겨서 동작은 맞지만 import 와 `private fun Section`(`:567`)이 죽은 코드다.
  **PR #68 이 이 자리를 건드리므로 그게 들어온 뒤에 정리한다**
- **강제 종료된 산책 복구 화면이 없다.** 원본 Room 세션은 닫지 않은 채 남겨 데이터는
  보존하지만, 다음 실행에서 이어 기록하거나 폐기할 사용자 흐름은 아직 만들지 않았다
- **새 소품이 기존 사용자에게 안 보인다.** `HomeScreen` 이
  `store.loadItems() ?: STARTER_ROOM` 이라, 한 번이라도 앱을 연 적이 있으면 저장된
  배치를 읽고 `STARTER_ROOM` 을 안 본다. 그래서 나중에 추가한 소품은 방이 아니라
  인벤토리에 들어간다. 앱을 지우고 다시 깔면 보인다. **소품을 추가할 때마다 밟는다**
- **에뮬레이터가 안 뜬다.** 스냅샷 복귀로 켜면 `screencap` 이 빈 버퍼(~26KB)를
  돌려주고(`uiautomator dump` 로는 화면이 그려지는 게 확인된다), 콜드 부팅은
  `adb shell` 이 멈춘다. 화면 확인은 지금 실기기뿐이다
- 릴리스 빌드에 R8 이 꺼져 있어 안 쓰는 리소스가 안 벗겨진다. 앱 크기를 줄여야 할
  때 볼 자리다

## 참고

- 장소(병원·약국·카페 등) 지도와 검색 패널·결과 카드·길찾기/전화 동작은
  **이 저장소로 들어왔다** (HISTORY.md 17~18절). 원본은
  `rkbuhtig/DAENGS_geo` `main@c5f0d5f` 로 고정했고 geo 사본은 동결이다.
  사용자 강아지 등록·선택은 이 이주에 넣지 않았고, 나중에 따로 만들었다
- 산책 기록 코어·Room 원본 로그·네이버 경로 레이어·Foreground Service·제어 UI는
  `rkbuhtig/DAENGS_geo@a145375927ce2372de376475f85e1377eeaf0168`에서 앱 경계에 맞춰
  다섯 PR로 나눠 옮겼다. 서버 업로드·영토·리플레이는 가져오지 않았다
- **`/v2/places/search` 와 `/journey` 는 이제 배포된 서버에 있다.** 실기기에서 내
  주변 20곳이 실제로 내려온다. 한동안 그 경로가 없어 검색이 404 였는데, 저쪽에서
  붙였다
- 기획·결정 사항은 [`CONTEXT.md`](CONTEXT.md), 만들어온 과정은
  [`HISTORY.md`](HISTORY.md) 에 있다
